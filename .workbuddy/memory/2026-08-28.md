# 2026-08-28 工作日志

## goodwe 代码审计：缺失模块 = 本地 checkout 子集，非"项目没做"

- 用户提出"是不是那部分代码我这边没有"——验证成立。
- 证据：① `G:\Project\goodwe` 无 `.gitmodules`、`git submodule status` 为空，是多模块拼装的**本地子集**；② nacos 导出（`nacos/nacos_config_export_20250519141610/.metadata.yml`）含大量 dataId：`secp-iot-mqtt-auth`、`secp-iot-cec102-gate/manager/process`、`secp-message-convert`、`middle-forward`、`middle-edge-gw`、`middle-datacenter-service`、`middle-route-gw`、`middle-simulate-data`、`middle-baobi-forwarding` 等，但 `project/`、`baobi/` 下无这些模块源码 → **服务确凿存在，只是源码没 clone 到本地**。
- 结论（重要认知修正）：之前几轮把"Flink 落库 / 设备遥测 MQTT 入站 / 边缘 publish / 接入层"缺失当成"简历夸大/未核实"是**错的**，应撤回。正确判定：① 数仓=prophet/ClickHouse（消费 Kafka dwh topic，✅ 已坐实）；② PG/TimescaleDB `_t0/_t1` 是聚合曲线产物（✅）；③ Flink 不存在（✅ Spring Kafka+调度）；④ 接入/转发层、边缘 MQTT publish 端 = 🔶 本地 checkout 不含源码，架构真实存在，讲不了内部实现。
- 面试策略：简历"设备 MQTT → 数仓"链路**可信**，nacos 配置佐证全链路；讲细节时只避免拍胸口说"我亲手改过那块代码"。

## 文档维护
- 已更新《MQTT设备数据入数仓_面试实现详解.md》：第3行加"关键认知"段，第四章补 nacos dataId 铁证+子集判断，第十章审计表把"未核实/删去"改为"🔶 本地 checkout 不含源码、架构存在不否定"，并加"🔶 含义重申"撤回先前误判。
- 新建《数据清洗_链路实现详解.md》：用户明确"需要的是如何清洗里面的数据"，补齐真实数据清洗(ETL)五层逻辑（覆盖简历"Flink 清洗去重"误称）。
  - 真实清洗点(✅ 已坐实代码)：① 边缘 formula 引擎(Rust,寄存器→物理量)；② prophet DW*DataSource(反序列化+status null 空值过滤+SN 布隆过滤+解析插件字段关联+正则校验+ES 主键幂等去重+ES 失败过滤)；③ algorithm(Redis 调度幂等+5min聚合+T+1收敛)；④ data-config(MakeupRuleEnum: COPY/AVG/PERCENT/AI 补录 + DwMissingDataInfo 缺失检测 + InconsistentTagEnum 异常标签)。
  - 接入层(secp-iot-*/message-convert)清洗🔶 源码未含，仅讲职责。
- 新建《数据清洗方案设计文档.md》：用户要求**独立写一套清洗方案设计**（不依赖本地代码），贴合其存储习惯——数仓后端用 **PG/TimescaleDB 时序库**（测试即查此处）。链路=设备上传→数仓清洗→框架→业务，业务有时直调数仓接口。含分层清洗(L0校验/L1标准化/L2去重/L3质量/L4维度补全/L5落库)、TD超表 DDL、数仓统一查询 API、框架领域服务、测试核对 SQL、质量监控指标、FAQ。
- 已修正《面试题_简历驱动版》7.4/7.6 节旧口径（用户确认"可以"）：
  - 7.4 由"Flink 流处理层"整体重写为"实时处理层（⚠️ SECP 真实实现：Spring Kafka + 定时调度，不是 Flink）"，含真实 Q&A（prophet DW*DataSource 消费 Kafka 数仓 topic、algorithm @Async 聚合写 PG/TimescaleDB、端到端不丢不重、分层就近清洗），原 Flink 知识降级为"通用知识储备"；
  - 7.6 顶部更正 + Q46-23/25/26 改为"数仓=ClickHouse（prophet 消费 Kafka `secp-rich-*-event-data`），PG/TimescaleDB 存聚合曲线产物"口径。
  - 至此三份文档（MQTT链路实现详解、数据清洗链路实现、面试题）口径全部一致。
- 新增《面试题_简历驱动版》7.7「MQTT 与物联网架构设计（综合追问）」整节：用户要求"大量 MQTT 面试题 + 物联网设计思考 + 安全认证/海量连接高可用/可靠性幂等/落到 SECP 真实代码 + MQTT 内业务清洗与存储逻辑(重点)"（4 选项全选 + 强调洗存逻辑要完善）。含 7.7.1 协议核心、7.7.2 架构设计、7.7.3 安全认证、7.7.4 海量连接高可用、7.7.5 可靠性幂等、7.7.6 MQTT 入站后业务清洗与存储完整链路(分层就近 L0-L5 + 落库分工 + 失败处理)，全部映射到 SECP 真实代码(secp-manager MqttConfiguration/MessageHandlerFactory、EMQX、prophet DW*DataSource、algorithm @Async 聚合、data-config 补录)。同步更新目录 TOC。
- **重写《数据清洗方案设计文档.md》**：用户给出真实架构口径，需按此校准——① 标准化(核心前提：相同类型对应相同数据，红外/遥感遥测/Modbus 同物理量归一到同一 `metric_key`，映射表驱动加设备零代码改动)；② 链路=IoT传感器→MQTT→标准化→Kafka→数仓(清洗+计算)→TSDB(TimescaleDB(tsdb库))→T+0实时/T+1经T+0再次计算→框架(也要复算同口径)→平台/业务；③ T+0/T+1 都在 TSDB(_t0/_t1 超表)，T+1 通过 T+0 重算收敛迟到/重复/补录；④ 维度层级 **场站-系统-租户-设备**（统一标签，rollup 设备→系统→场站→租户）；⑤ 框架层不是只封装，而是复用数仓计算口径在平台侧按业务维度复算(收益/碳资产等)，业务可直调数仓接口旁路。测试核对直接查 PG/TimescaleDB。该文档现为独立于本地代码的真实架构蓝图。
- 新增《面试题_简历驱动版》7.8「TimescaleDB 时序库深度追问」整节（用户要求"tsdb 面试需要更全一点"）：覆盖 7.8.1 时序数据特征/为何不用普通表与分库分表(hypertable 替代 ShardingSphere)、7.8.2 hypertable+chunk+分区裁剪原理/chunk_time_interval/索引、7.8.3 连续聚合(对应 T+0/T+1 原生机制)、7.8.4 列式压缩(segmentby/orderby)、7.8.5 drop_chunks/TTL/分层、7.8.6 攒批UPSERT幂等/写入热点、7.8.7 查询性能+慢查排查(EXPLAIN)+分区裁剪失效、7.8.8 选型对比表(TimescaleDB vs InfluxDB/ClickHouse/TDengine)+TSDB与ClickHouse边界、7.8.9 SECP 真实证据速记(station_pv_power_5min_t0/t1)。同步更新目录 TOC。
