# 2026-08-18 工作日志

## Java 基础面试文档（固德威业务场景版）

- 编写了 `Java基础面试_固德威业务场景版.md`，结合 pdai.tech Java 全栈知识体系与固德威 SECP 平台实际代码
- 覆盖四大模块：Java 集合框架、Java 多线程与并发、Java IO/NIO、JVM
- 每个知识点都引用了 SECP 项目中的真实代码片段（UserThingPermissionCache、TagMismatchCollector、BizFileServiceImpl、BusinessLockAspect、ThreadPoolConfig 等）
- 包含 4 个综合面试场景题（千万级设备数据采集、结算幂等性、线程池管理、线上排查）
- 数据来源：goodwe 项目 39000+ Java 文件 + pdai.tech JVM/IO/JUC 知识体系

## 追加设计模式模块

- 在 `Java基础面试_固德威业务场景版.md` 中追加第五章"设计模式"
- 覆盖 10 种设计模式，均结合 SECP 真实代码：
  - 策略模式 → AtomicQueryNodeDataContext + ElectricityTemplateTypeInterface（7 种电价模板）
  - 模板方法 → AbstractOrderGenerator + AbstractCommonPostProcessor
  - 责任链 → ProcessorChain（邮件→APP→Web→短信推送链）+ AbstractCommonPostProcessor
  - 代理模式 → BusinessLockAspect 等 8+ AOP 切面
  - 外观模式 → 20+ FacadeServiceImpl
  - 观察者模式 → Spring ApplicationEvent（SettlementApproveEvent 等 7 个事件）
  - 单例模式 → MapStruct INSTANCE + Spring @Component
  - 工厂模式 → SettlementStrategyFactory + MessageHandlerFactory
  - 建造者模式 → Lombok @Builder + Guava CacheBuilder
  - 适配器模式 → DaHuaMonitorAdapter + HikvisionMonitorAdapter
- 追加设计模式组合矩阵和高频面试问题
- pdai.tech 链接表追加 12 个设计模式相关链接

## 追加数据库模块

- 在 `Java基础面试_固德威业务场景版.md` 中追加第六章"数据库（PostgreSQL / MySQL）"，原综合场景题改为第七章并新增场景五
- 关键发现：SECP 主库是 PostgreSQL（schema 分域），不是 MySQL；时序用 TimescaleDB（create_hypertable 按天 chunk）
- 覆盖 8 个小节：B+树索引与最左前缀（tenant_id 联合索引）、事务与 @Transactional 失效场景、HikariCP 连接池（maxLifetime 与 wait_timeout）、MyBatis-Plus（#{} vs ${}、分页拦截器、批量插入、upsert 两种语法）、dynamic-datasource 多数据源读写分离（@DS + ThreadLocal）、TimescaleDB 时序设计、慢 SQL 排查（p6spy + EXPLAIN）、MySQL vs PG 对比
- 场景五：几十亿条设备 5min 功率数据的存储选型（TimescaleDB vs MySQL 分库分表）
- pdai.tech 链接表追加 SQL/MySQL/Redis 链接

## 追加 Redis 和消息队列模块

- 在 `Java基础面试_固德威业务场景版.md` 中追加第七章"Redis"和第八章"消息队列（Kafka/RocketMQ）"，原综合场景题改为第九章
- Redis 章 6 小节：数据结构与底层实现（SDS/跳表 + StringRedisSerializer 选型）、缓存三剑客（穿透/击穿/雪崩 + HikvisionComponent 击穿防护）、缓存与 DB 一致性（token 缓存 expiresIn-1 防御性编程）、Redisson 分布式锁（watchdog 10s + BusinessLockAspect）、Redis Pub/Sub（RedisMsgSendUtil + ThingListener 跨服务缓存失效）、持久化与高可用
- MQ 章 5 小节：MQ 三大作用与风险、Kafka 原理（acks=all + 零拷贝 + 批量消费 + MD5 幂等去重）、RocketMQ 原理（延迟消息 syncDelaySend + Tag 过滤 + 死信队列 + 与 Kafka 对比）、消息可靠性（不丢/不重/顺序/积压/DLQ）、4 个 SECP 真实 MQ 场景（资产分享联动延迟消息、物操作联动模板、操作日志管道幂等、数据仓库事件源多 Topic 分流）
- 关键发现：SECP 同时用 RocketMQ（业务事件）+ Kafka（数据管道）；用户本人写了 AssetShareUpdateProducer（syncDelaySend 延迟消息）
- pdai.tech 链接表追加 Redis/Kafka/RocketMQ 链接
- 文档总行数 2778 行，覆盖 8 大模块 + 5 个综合场景题

## 扩展集合 + Spring/微服务/网络/Netty 四大新章

- 第一章新增 1.6-1.10 五节：TreeMap（YongSignHelper API 签名按字典序 + GenerateAnalysisServiceImpl 区间合并）、TreeSet 去重排序、LinkedHashMap（ContractServiceImpl 保序 + groupingBy LinkedHashMap::new + 手写 LRU）、HashSet/hashCode/equals、Queue 家族（ElectricMeterUpdateRatedPowerPlugin 的 LinkedBlockingQueue、ArrayBlockingQueue/SynchronousQueue 线程池选型、HikvisionFacadeServiceImpl 的 RBlockingQueue 云台排队）、fail-fast 与 removeIf
- 新增第九章 Spring/Spring Boot/Spring Cloud：Bean 生命周期、三级缓存、AOP（8+ 切面）、自动装配（goodwe-core-spring-boot-starter 的 spring.factories 一手代码）、Spring Cloud 组件落地表（Nacos/OpenFeign+TraceIdInterceptor/灰度 extproc）、@Async + MdcTaskDecorator 的 MDC 传递
- 新增第十章 微服务与分布式：SECP 30+ 服务域划分、通信选型三分法（Feign/MQ/Pub-Sub）、为什么不用 Seata（本地事务+事件+延迟消息+补偿 topic）、自研链路追踪（TraceIdFilter→MDC→Feign 拦截器→Agent）、幂等四层汇总、分布式 ID
- 新增第十一章 计算机网络：三次握手结合 MQTT 长连接、粘包拆包结合 MQTT 剩余长度字段、TLS 握手结合 MqttConfiguration 的 SSLContext/TrustManager 代码、URL 到页面用 SECP 链路回答
- 新增第十二章 Netty 与 MQTT：Reactor/ByteBuf/Pipeline 速答表、MQTT QoS 与 SECP 场景对应、Paho+Spring Integration 出站代码、为什么业务用 Paho 而非直接 Netty 的边界论述
- 综合场景题顺延为第十三章，文档共 3370 行、13 章 + 5 场景题
- pdai 新增链接：spring-x-framework-aop、springboot 知识体系、outline（已验证存在；spring-x-series 和 netty-x-introduce 是 404，勿用）

## 追加 Java 8+ 新特性 / 安全认证 / DevOps 三章

- 新增第十三章 Java 8+ 新特性：Lambda 与函数式接口（Function/Consumer/Supplier/Predicate/BiFunction + CacheRedisServiceImpl 设备列表 filter-map-distinct）、Stream API（5 段 SECP 真实代码：toMap merge function、joining、groupingBy LinkedHashMap、toSet + parallelStream 问答表）、Optional（StationIndicatorEnum 链式查找、orElseThrow 策略工厂、CompletableFuture+Optional 优雅降级）、方法引用（4 种 + 构造方法引用 LinkedHashMap::new）、默认方法（BaseMapper 批量转换）、java.time（全平台 LocalDateTime 替代 Date、Duration 替代手工毫秒）、其他新特性速答表
- 新增第十四章 安全认证与 OAuth2：认证 vs 授权、OAuth2 四种授权模式（GrantTypesConstant 一手代码 + DaHuaFacadeServiceImpl 客户端模式 + OAuthController Token 端点）、JWT 原理与 JWT vs Session 对比、Token+Redis 会话管理（TokenRedisDao 完整代码：Web 4h TTL + App 不过期 + Hash 多设备管理 + 远程登出 + 续期机制）、Feign 令牌传递（SecurityInterceptor 完整代码：RequestContextHolder 子线程坑 + FeignContextHolder 解决方案）、SECP 安全架构全景图、高频问答
- 新增第十五章 DevOps 与容器化：Docker 基础（Dockerfile 原文分析：统一基础镜像+jemalloc+SkyWalking Agent+OOM dump+环境变量控制 profile）、容器 JVM 调优（Xms=Xmx 原因、容器 OOM Killed 排查 = 堆外内存）、SkyWalking 链路追踪（Agent 字节码增强 + SupplierWrapper 异步 TraceId + 拓扑图 + 告警）、jemalloc 内存分配器（glibc vs jemalloc 碎片对比 + LD_PRELOAD 替换）、高频问答
- 综合场景题顺延为第十六章，文档共 4244 行、16 章 + 5 场景题
- pdai 链接表追加 Java 8 新特性/Stream/Lambda/Optional/Docker/K8s 链接

## 新增数据结构·树章节

- 在 `Java基础面试_固德威业务场景版.md` 中新增第十六章"数据结构·树"，原综合场景题顺延为第十七章
- 10 小节覆盖：二叉树遍历（前/中/后/层序）、BST 性质与 TreeMap 签名场景、AVL vs 红黑树对比（HashMap 选红黑树原因 + 五大性质）、B/B+ 树与 6.1 串联、堆与 PriorityQueue（数组存储 + siftUp/siftDown + SECP 告警优先级潜在应用）、Trie 前缀树（SECP 设备编码自动补全场景）、N 叉树（4 个 SECP 真实实体：TopologyTreeNode/DeptInfo/PermissionInfo/TopologyNodeInfo + 邻接表存储模型对比）、BFS vs DFS（SniperFacadeServiceImpl BFS 建树 + SettlementMeterServiceImpl BFS 找 GRID 节点 + PermissionServiceImpl DFS 递归删子节点 + MetricCompensationServiceImpl DFS 搜索功率因数剪枝）、线段树与树状数组速答、高频问答
- 4 段 SECP 真实代码完整引用：buildStationTopologyTreeNode 的 Queue BFS、setPowerAccount 的 Queue BFS、removeChildren 递归、filterGridIdPfValueMap DFS 剪枝
- pdai 链接表追加数据结构与算法总览、树结构基础链接
- 文档总行数 4805 行，17 章 + 5 场景题

## 扩充 6.8 MySQL vs PostgreSQL 深度对比

- 原有 6.8 节仅 24 行（一张 7 维对比表 + 一段总结），扩展为 6 个子节（6.8.1~6.8.6）
- 6.8.1 核心对比表扩充到 14 维（新增隔离级别默认/间隙锁/自增主键/DDL语法/索引类型/复制/存储引擎）
- 6.8.2 PG 独有特性真实代码 5 个：WITH RECURSIVE 递归 CTE（ThingInfoDao 设备树递归查询）、ROW_NUMBER() OVER PARTITION BY 窗口函数（SolarTheoryEnergyDataDao 去重取最新）、ON CONFLICT DO UPDATE SET EXCLUDED（LocationWeatherNowTodayDao 天气 upsert）、JSONB+GIN（request_logs DDL）、schema 分域（nacos 7 个 currentSchema 真实配置）
- 6.8.3 MVCC 实现差异对比表（旧版本存储/版本可见性/清理机制/回滚/长事务影响）
- 6.8.4 完整选型逻辑（30 秒版 + 2 分钟展开版，4 个驱动力 + 什么时候仍选 MySQL 反向思考）
- 6.8.5 MySQL 八股速答（redo/undo/binlog/两阶段提交/Buffer Pool/Change Buffer/Next-Key Lock/MVCC）
- 6.8.6 PG 速答（VACUUM/pg_stat_statements/WAL/SEQUENCE/CTE 物化/EXPLAIN ANALYZE）
- 文档总行数 5016 行
