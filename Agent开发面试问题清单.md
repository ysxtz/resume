# Agent 开发面试问题清单

基于简历中的"运维智能助手 Agent"项目，整理面试官可能问到的问题。

---

## 一、架构设计类

### 1. 为什么选择 Spring AI + LangChain4j？
**可能回答要点：**
- Spring AI：与 Spring Boot 生态无缝集成，支持依赖注入、配置管理
- LangChain4j：Java 原生的 LLM 框架，提供 Agent、RAG、Function Calling 等能力
- 团队技术栈统一（都是 Java/Spring）
- 对比其他方案：Python 的 LangChain 需要跨语言调用，Go 的框架生态不成熟

### 2. Agent 整体架构是怎样的？
**建议画图说明：**
```
飞书消息 → 消息解析 → Agent 编排层
                        ↓
                    LLM 推理（通义千问/GPT）
                        ↓
                    Function Calling
                        ↓
            ┌───────────┼───────────┐
            ↓           ↓           ↓
        告警查询     设备状态     知识库检索
            └───────────┼───────────┘
                        ↓
                    响应生成 → 飞书回复
```

### 3. 单 Agent 还是多 Agent？为什么？
**回答思路：**
- 单 Agent：简单场景，一个 LLM 处理所有任务
- 多 Agent：复杂场景，不同 Agent 负责不同领域（告警分析 Agent、运维执行 Agent、报告生成 Agent）
- 实际选择：初期单 Agent，后期根据任务复杂度拆分

---

## 二、RAG 实现类

### 4. RAG 是怎么实现的？
**回答要点：**
- 知识库来源：运维 SOP、历史故障案例、设备手册
- 文档切分：按段落/章节切分，保留上下文
- 向量化：使用 Embedding 模型（如 text-embedding-ada-002）
- 存储：Elasticsearch（已有）或 Milvus/Pinecone
- 检索：相似度匹配 Top-K，结合关键词过滤
- 重排序：Cross-Encoder 模型二次排序

### 5. 向量检索的召回率和准确率怎么保证？
**回答思路：**
- 混合检索：向量检索 + 关键词检索（BM25）
- 元数据过滤：按设备类型、告警级别过滤
- 重排序：Cross-Encoder 模型
- 评估指标：Recall@K、Precision@K
- 持续优化：收集运维人员反馈，调整检索策略

### 6. 知识库更新频率是多少？怎么保证时效性？
**回答思路：**
- 增量更新：新故障案例、新设备手册实时更新
- 定期全量：每月全量重建索引
- 版本管理：知识库版本化，支持回滚
- 质量审核：运维专家审核后入库

---

## 三、Function Calling 类

### 7. Function Calling 是怎么实现的？
**回答要点：**
- 定义函数：告警查询、设备状态查询、指标查询、Runbook 执行
- LLM 调用：LLM 根据上下文决定调用哪个函数
- 参数提取：LLM 提取函数参数（如设备 ID、时间范围）
- 执行函数：调用内部 API
- 结果返回：将执行结果返回给 LLM，生成最终回复

### 8. 函数调用的安全性怎么保证？
**回答思路：**
- 权限控制：基于 RBAC，不同用户可调用不同函数
- 参数校验：严格校验 LLM 提取的参数
- 执行审批：高风险操作（如重启设备）需要人工确认
- 审计日志：记录所有函数调用

### 9. 如果 LLM 幻觉调用了不存在的函数怎么办？
**回答思路：**
- 函数注册表：只允许调用预注册的函数
- 参数校验：严格校验参数类型和范围
- 错误处理：捕获异常，返回友好提示
- 兜底策略：无法处理时转人工

---

## 四、告警智能分类类

### 10. 告警智能分类是怎么实现的？
**回答思路：**
- 告警内容：包含设备 ID、告警级别、告警描述、时间
- Prompt 设计：让 LLM 分类告警类型（设备故障、网络异常、性能告警等）
- Few-shot：提供历史告警分类示例
- 后处理：LLM 分类结果后处理，映射到内部告警类型

### 11. 分类准确率是多少？怎么提升？
**回答思路：**
- 初始准确率：70-80%
- 优化后：90%+
- 优化方法：
  - 优化 Prompt（更清晰的分类规则）
  - Few-shot 示例
  - Fine-tuning（如果有足够训练数据）
  - 集成多个模型投票

---

## 五、根因分析类

### 12. 根因分析是怎么实现的？
**回答思路：**
- 数据收集：收集告警前后的设备状态、指标数据
- 关联分析：找出同时发生的告警
- LLM 推理：基于历史故障案例和 SOP，推理可能的根因
- 建议生成：给出排查步骤和修复建议

### 13. 如果根因分析错了怎么办？
**回答思路：**
- 人工确认：运维专家审核 LLM 的分析结果
- 反馈机制：运维人员标记正确/错误的分析
- 持续学习：收集反馈，优化 Prompt 和知识库
- 兜底策略：无法确定时，给出多个可能的根因

---

## 六、Runbook 自动执行类

### 14. Runbook 自动执行是怎么实现的？
**回答思路：**
- Runbook 定义：JSON/YAML 格式，包含步骤、命令、参数
- 执行引擎：解析 Runbook，逐步执行
- 条件判断：根据上一步结果决定是否继续
- 错误处理：失败时重试或回滚
- 人工确认：高风险步骤需要人工确认

### 15. 怎么保证 Runbook 执行的安全性？
**回答思路：**
- 沙箱环境：在隔离环境执行
- 权限控制：最小权限原则
- 执行审批：高风险操作需要人工确认
- 回滚机制：失败时自动回滚
- 审计日志：记录所有执行步骤

---

## 七、飞书机器人对接类

### 16. 飞书机器人是怎么对接的？
**回答思路：**
- 飞书开放平台 API：创建机器人应用
- 消息接收：Webhook 回调
- 消息发送：调用飞书 API
- 卡片消息：使用飞书卡片模板，展示结构化信息
- 事件订阅：订阅消息事件

### 17. 多轮对话是怎么管理的？
**回答思路：**
- Session 管理：基于用户 ID 维护会话上下文
- 上下文存储：Redis 存储对话历史
- 上下文窗口：保留最近 N 轮对话
- 摘要压缩：对话历史过长时，压缩为摘要

---

## 八、性能优化类

### 18. Agent 的响应时间是多少？怎么优化？
**回答思路：**
- 初始响应时间：5-10 秒
- 优化后：1-3 秒
- 优化方法：
  - 流式输出：LLM 生成过程中实时返回
  - 缓存：相似问题缓存结果
  - 并行调用：多个 Function Calling 并行执行
  - 异步处理：非实时任务异步执行

### 19. LLM 调用的成本怎么控制？
**回答思路：**
- 模型选择：简单任务用小模型，复杂任务用大模型
- Prompt 优化：减少 Token 消耗
- 缓存：相似问题缓存结果
- 限流：限制单用户调用频率

---

## 九、实际效果类

### 20. 上线后效果怎么样？
**回答思路：**
- 告警诊断时间：从 5 分钟缩短至 30 秒
- 运维效率提升：40%
- 日均处理告警：2000+ 条
- 用户满意度：运维人员反馈积极

### 21. 遇到过什么挑战？
**可能挑战：**
- LLM 幻觉：生成不准确的信息
- 响应延迟：LLM 调用慢
- 成本控制：Token 消耗高
- 用户接受度：运维人员不信任 AI
- 知识库质量：历史数据不完整

---

## 十、未来规划类

### 22. 未来有什么优化方向？
**回答思路：**
- 多 Agent 协作：拆分更细粒度的 Agent
- Fine-tuning：基于历史数据微调模型
- 主动预警：基于历史数据预测故障
- 知识图谱：构建设备故障知识图谱
- 自学习：自动从运维反馈中学习

---

## 面试准备建议

### 1. 准备一个完整的 Demo
- 准备一个可演示的 Demo（可以录屏）
- 展示飞书机器人对话、告警分类、根因分析、Runbook 执行

### 2. 准备技术细节
- 熟悉 Spring AI + LangChain4j 的核心 API
- 熟悉 RAG 的实现细节
- 熟悉 Function Calling 的工作原理

### 3. 准备数据支撑
- 准备好具体的性能数据（响应时间、准确率、效率提升）
- 准备好用户反馈数据

### 4. 准备问题应对
- 思考"LLM 幻觉"的解决方案
- 思考"成本控制"的解决方案
- 思考"安全性"的解决方案

### 5. 准备延伸问题
- 面试官可能问到：RAG vs Fine-tuning、Prompt Engineering 技巧、LLM 评估方法等
- 准备相关的知识储备

---

## 常见追问

1. **为什么不用 Python 的 LangChain？**
   - 团队技术栈统一（Java）
   - LangChain4j 功能足够
   - 与 Spring Boot 集成更顺畅

2. **RAG 的向量检索用的什么模型？**
   - text-embedding-ada-002（OpenAI）
   - 或开源模型（如 BGE、M3E）

3. **Function Calling 的函数定义是怎么写的？**
   - JSON Schema 格式
   - 包含函数名、描述、参数定义

4. **Prompt 是怎么设计的？**
   - System Prompt：定义 Agent 角色和行为规范
   - User Prompt：包含用户问题和上下文
   - Few-shot：提供示例

5. **怎么评估 Agent 的效果？**
   - 准确率、召回率
   - 响应时间
   - 用户满意度
   - A/B 测试

---

祝面试顺利！
