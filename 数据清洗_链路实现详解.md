# 设备数据清洗（ETL）链路实现详解

> **定位**：简历里"设备 MQTT → 数仓"链路上一句"Flink 清洗去重"是**误称**（全仓库零 `org.apache.flink`）。本文补齐真实的数据清洗（ETL）逻辑，基于 `G:\Project\goodwe` 真实代码审计（2026-08-27~28 多轮核实）。
> 标注档位：✅ 已坐实（本地有源码，可逐行讲）/ 🔶 源码未在本 checkout（架构存在，nacos 佐证，讲不了内部）。
>
> **⚠️ 关键认知**：`G:\Project\goodwe` 只是 goodwe 仓库的**本地子集**（无 `.gitmodules`、无 submodule），接入层的 `secp-iot-*`/`secp-message-convert`/`middle-*` 源码不在你这边（nacos 配置导出里有它们的 dataId 佐证存在）。本文 🔶 环节 = "源码没拉全"，**不等于项目没做**。

> **一句话口径（面试先亮这个）**：清洗不是某一步 Flink 作业，而是**分层就近清洗**——① 边缘 Rust 采集时就把裸寄存器换算成物理量（formula 引擎）；② 接入层做协议归一（源码未含）；③ 云端 `secp-prophet` 消费 Kafka 数仓 topic 时做反序列化 + 空值过滤 + 设备白名单过滤 + 解析关联 + ES 主键幂等去重 + 落库失败过滤；④ `secp-algorithm` 做 5min 窗口聚合 + 迟到数据 T+1 收敛；⑤ `secp-data-config` 做缺失检测 + 补录（复制/均值/百分比/AI）+ 异常标签。

---

## 一、边缘侧清洗：裸寄存器 → 物理量（✅ Rust / iot）

最容易被忽略但最关键的"清洗"发生在设备旁边，不是在云端。

- 设备吐的是**裸 Modbus 寄存器**（如 `0x1234` 低 8 位是一个字节、某几位拼成一个数），不是"功率 3520W"。
- `sebu-edge-collector` 的 **`formula` 引擎**（`sebu-edge-formula`，`rust_decimal` 高精度）负责换算：
  - `@/$` 变量引用：从其他寄存器取值参与运算
  - `mergeRegister`：跨寄存器拼值
  - `getBits` / `bitScale`：位提取 + 缩放（解决"某位是个标志位"的问题）
- 输出是 `Vec<TsData>{timestamp, sn, key, value}`——**上行到云端的是已经算好的物理量**，不是原始报文。
- 这一步清洗了什么：单位不一致、位域错位、符号/缩放错误、精度丢失。

> 面试话术："我们做的是**边缘计算**，清洗前移到采集器——寄存器级原始数据在边缘就被 formula 引擎换算成标准物理量再上行，云端拿到的是干净数据，既省带宽又减轻云端计算。"

---

## 二、接入层清洗：协议归一（🔶 源码未在本 checkout）

- 负责方：`secp-iot-cec102-*`、`secp-message-convert`、`middle-forward` 等（nacos 配置里有 dataId 佐证存在）。
- 职责推断：把不同协议（Modbus/HTTP/MQTT/IEC104/国标 CEC102）的设备报文**归一化成统一的 Kafka 消息结构**，再投到 `secp-rich-*-event-data` 数仓 topic。
- 这一层做：协议适配、字段映射、时间戳归一、编码转换。
- **本地无源码**，面试只讲"职责与产物"，不报内部实现。

---

## 三、数仓 prophet 侧清洗（✅ 核心，代码可逐行讲）

设备事件/状态/运行类数据进 ClickHouse（数仓）前的清洗，全部在 `pcg-secp-prophet` 里，链路：`Kafka dwh topic → DW*DataSource → 解析插件链 → ES/ClickHouse`。

### 3.1 入站即过滤（DWEventDataSource.convert）

`biz/data/datasource/mq/kafka/dwh/event/DWEventDataSource.java`：

```java
List<DWEventMessage> messageList = list.stream().map(rec -> {
    DWEventMessage value = JacksonUtils...fromJsonString(rec.value(), DWEventMessage.class); // 反序列化
    value.setKafkaUuid(simplyTopic(rec.topic()) + "-" + rec.partition() + "-" + rec.offset()); // 唯一追踪号
    return value;
}).filter(dw -> Objects.nonNull(dw.getStatus()))          // ① 空值过滤：status 为 null 的脏数据直接丢
  .collect(toList());
if (needSnFilter) {
    messageList = messageList.stream()
        .filter(m -> snFilter.mightContain(m.getSn()))     // ② 设备白名单：布隆过滤器挡掉未知设备
        .collect(toList());
}
log.info("filter-rate before: {} after: {}", list.size(), messageList.size()); // ③ 过滤率监控
return DWEventMessageMapper.toUnifiedEventMessage(messageList); // 映射成统一消息体
```

**三层清洗**：反序列化失败捕获 → status 空值过滤 → SN 布隆过滤。

### 3.2 解析插件链做字段关联补全

`handleMessage()` → `dispatch2Parser()` → `AbstractMessageParser` 的插件链（`setPluginList()`）：

```
deviceParserPlugin → stationParserPlugin → eventParserPlugin → messageParserPlugin → nvrRedirectParserPlugin
```

每个插件把"设备码/站点/事件编码"等**关联补全**成 `UnifiedMessage`（统一消息体）。`UnifiedMessage.pushValid` 字段由 `MessageParserPlugin` 填充，校验"发送时间段、发送对象"是否合法（校验不通过 = false，下游不推送）。

### 3.3 落库层清洗：ES 主键幂等去重 + 失败过滤（afterParse）

`AbstractMessageParser.afterParse()`（第 84~139 行）：

```java
List<EsEventMessagePO> pos = esEventMessagePOConverter.fromErrCodeMessage(messageList);
BulkResponse bulk = esOperateService.saveEventMessage(pos);          // 批量写 ES
if (bulk.hasFailures()) {
    List<String> failedIds = Arrays.stream(bulk.getItems())
        .filter(BulkItemResponse::isFailed).map(BulkItemResponse::getId).collect(toList());
    // ④ 落库失败过滤：把写 ES 失败的消息剔除，只把成功的推下游 Kafka
    pos = pos.stream().filter(p -> !failedIds.contains(p.getId())).collect(toList());
}
// ⑤ 条件过滤：只有"恢复类事件且 originId/deviceCode/eventCode 齐全"才关联发生消息
if (EVENT_RECOVERY.equals(po.getStatus()) && po.getOriginId()!=null && ...) {
    po.setRelatedOccurMessageId(...);
}
platformKafkaTemplate.send(postProcessTopic, po.getDeviceCode(), po);
```

注释明写："失败可能是重复消费主键重复导致插入失败，这种情况不用处理"——**即幂等去重靠 ES 文档主键（kafkaUuid）**，重复消息写 ES 直接失败被丢弃，不会重复进下游。

### 3.4 格式校验（MessageRuleInfo 正则）

`secp-prophet-interface` 的 `MessageRuleInfo` 定义了一组**正则校验**：消息编码、标题、可能原因、解决方案、内容格式（字段 `消息编码格式校验正则`/`消息标题格式校验正则`/...）。解析时按规则校验，不匹配的走异常处理（错误码消息由 `ErrCodeMessageParser` 单独处理）。

**prophet 侧清洗总览**：反序列化 → 空值过滤(status null) → SN 布隆过滤 → 解析插件字段关联 → 正则格式校验(pushValid) → ES 主键幂等去重 → ES 失败过滤 → 条件关联。一句话：**边消费边清洗，脏数据在进数仓前就层层截掉**。

---

## 四、时序 algorithm 聚合清洗（✅ 聚合 + 迟到收敛）

`pcg-secp-algorithm` 的 `StationMetricScheduleServiceImpl.generateRt5MinCurves()`（`@Async` 调度）：

- 从 `secp-watchman` 拉 5min 曲线 → `stationPvPower5minT0Repository.saveBatch(pvList)` 写 PG/TimescaleDB `_t0` 表（聚合曲线产物，非原始遥测）。
- **去重/收敛**：用 Redis 记上次执行时间（`lastExecuteTime`），只处理该时刻之后的数据——天然跳过已处理区间（幂等）。
- **迟到数据**：T+1 调度重算昨日 `_t1` 表，覆盖补齐迟到/重复数据（最终一致）。

这一层清洗的是：**窗口聚合（原始点→5min 曲线）+ 调度幂等去重 + 迟到数据 T+1 收敛**。

---

## 五、data-config 数据质量清洗（✅ 缺失检测 + 补录 + 异常标签）

`secp-data-config` 是专门的数据质量模块，处理时序数据的"缺失 / 异常 / 不一致"：

- **缺失检测**：`DwMissingDataInfo` / `DwMissingDataDetail` —— 记录数仓缺失数据（哪些设备哪些时段缺数、缺失率）。
- **补录规则** `MakeupRuleEnum`（4 种策略，真实枚举）：
  - `COPY` 复制（用相邻时段/同型号设备数据复制）
  - `AVG` 平均值（时段均值填充）
  - `PERCENT` 百分比（按比例推算）
  - `AI` 智能（模型预测填充）
- **补录状态** `MakeupFlgEnum`：`UNMAKEUP`(未补录) / `PARTMAKEUP`(部分) / `ALLMAKEUP`(全补录) / `ORIGINDATA`(原始数据) —— 区分"原值"与"补录值"，下游分析可识别。
- **异常/不一致标签** `InconsistentTagEnum` / `ErrTagReq` / `ProjectErrData` —— 数据不一致（如发电量 vs 上网电量对不上）打标签分类。
- **周期偏差** `PeriodDeviationTypeEnum`：`TOTAL`(发电) / `SELF_USE`(自用) / `GRID`(上网) / 各类收益 —— 按维度检测周期偏差，定位异常来源。

这一层清洗的是：**识别缺失/异常 → 按规则补录（并在标志位区分原值/补录值）→ 给不一致数据打标签**——是时序数据进入结算/报表前的"最后一公里质量关"。

---

## 六、清洗分层对照表 + 面试话术

| 清洗层 | 位置 | 真实代码 | 清洗动作 | 标注 |
|--------|------|---------|---------|------|
| ① 边缘换算 | iot / formula(Rust) | `@/$`/`mergeRegister`/`getBits`/`bitScale` | 寄存器→物理量、单位/位域/缩放开源 | ✅ |
| ② 协议归一 | secp-iot-*/message-convert | nacos dataId 佐证 | 多协议→统一 Kafka 消息 | 🔶 |
| ③ 数仓入站 | prophet / DW*DataSource | status 空值过滤 + SN 布隆 | 反序列化、空值、白名单 | ✅ |
| ③ 解析关联 | prophet / AbstractMessageParser 插件链 | device/station/event plugin | 字段关联补全、正则校验 | ✅ |
| ③ 落库去重 | prophet / afterParse | ES 主键幂等 + 失败过滤 | 重复消息丢弃、失败剔除 | ✅ |
| ④ 聚合收敛 | algorithm / StationMetricSchedule | Redis 上次执行时间 + T+1 | 5min 聚合、调度幂等、迟到收敛 | ✅ |
| ⑤ 质量补录 | data-config | MakeupRuleEnum / DwMissingDataInfo | 缺失检测、COPY/AVG/PERCENT/AI 补录、异常标签 | ✅ |

**面试标准话术（替换掉"Flink 清洗去重"）**：

> "我们没有用 Flink 做清洗——实时链路是 Spring Kafka 消费 + 调度。清洗是**分层就近**做的：边缘采集器用 formula 引擎把裸寄存器换算成物理量，这是第一道清洗；云端 prophet 消费数仓 topic 时边消费边清洗——反序列化后先过滤 status 为空的脏数据、再用布隆过滤器挡掉未知设备，解析插件把设备/站点/事件字段关联补全并用正则校验格式，落 ES 时靠主键幂等去重、把写失败的消息剔除；算法服务做 5min 窗口聚合并用调度幂等 + T+1 收敛迟到数据；最后 data-config 专门做质量关——检测缺失数据、按复制/均值/百分比/AI 四种策略补录（并打标志位区分原值和补录值）、给不一致数据打标签。这样脏数据在进数仓前就被层层截掉，而不是靠一个流处理作业兜底。"

**高频追问**：
- Q：重复数据怎么处理？→ "两层：prophet 侧 ES 主键（kafkaUuid=topic-partition-offset）幂等，重复消费直接写失败被丢弃；algorithm 侧 Redis 记上次执行时间做调度幂等，迟到数据 T+1 重算收敛。"
- Q：缺数据怎么办？→ "data-config 检测缺失率，按 COPY/AVG/PERCENT/AI 四种规则补录，补录值用 MakeupFlgEnum 标记，结算/报表可识别原值 vs 补录值。"
- Q：为什么不用 Flink？→ "链路没引入 Flink，清洗需求用 Spring Kafka + 插件链 + 调度就能覆盖，少维护一个流处理组件；而且清洗是'就近分层'而非集中批处理，更符合边缘+云端的架构。"

---

## 七、与现有文档对应

| 本章 | 对应材料 |
|------|---------|
| 一、边缘 formula | `G:\Project\goodwe\iot`（前轮已审计） |
| 三、prophet 清洗 | `pcg-secp-prophet/.../dwh/*DataSource.java`、`AbstractMessageParser.java`、`UnifiedMessage.java` |
| 四、algorithm 聚合 | `StationMetricScheduleServiceImpl.java`（前轮已审计） |
| 五、data-config | `secp-data-config-interface` 枚举与实体（MakeupRuleEnum / DwMissingDataInfo / InconsistentTagEnum） |
| 二、接入层 | nacos 配置佐证（源码未含） |

> **注意**：《面试题_简历驱动版》7.4 节仍按"Kafka→Flink→清洗去重落库"写，与本文矛盾（无 Flink、清洗分五层）。建议同步改 7.4 节为本文口径。
