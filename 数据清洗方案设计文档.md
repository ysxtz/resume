# 物联网设备数据标准化与数仓计算方案设计（独立设计方案）

> **定位**：一套**独立的数据（标准化 + 清洗 + 计算）方案设计**，不依赖任何本地代码，用于回答"设备上传之后，数据怎么被标准化、进数仓、怎么算、再到框架与业务"。
> **关键链路**（你确认的真实架构）：`IoT 传感器/设备 → MQTT → 标准化(同类型同数据) → Kafka → 数仓(清洗+计算) → TSDB(TimescaleDB) → T+0 实时 / T+1 经 T+0 重算 → 框架(复算同口径) → 平台/业务`。
> **存储约定**：时序存 **PostgreSQL + TimescaleDB（tsdb 库）**——与团队测试期查数习惯一致，测试数据直接在 PG 时序库 `raw / dwd / dws / t0 / t1` 超表核对。
> **维度体系**：统一标签采用 **场站 - 系统 - 租户 - 设备** 四级层级，所有计算与查询按这套维度归集。

---

## 1. 背景与目标

### 1.1 问题
物联网设备（逆变器 / 储能柜 / 电表 / 采集器，含红外、遥感遥测等多种采集方式）通过 MQTT 上报的数据天然"脏"且"杂"：
- **杂**：同个物理量，不同型号/协议字段名不同、单位不同、时区不同（红外测温、遥测温度、Modbus 寄存器温度都是"温度"，但长相各异）
- **脏**：重复（QoS1 重传）、缺失（断网离线）、越界（负功率、超量程）、突变、恒定卡死
- **缺维度**：原始点位只有 `deviceId`，没有场站、系统、租户等业务归属

### 1.2 目标
构建"**设备上传 → 数仓（标准化+清洗+计算）→ 框架（复算）→ 业务**"的干净管道，使下游拿到**可信、可用、可查**的数据：
1. **可信**：脏数据在数仓层被分层截掉，落库即干净
2. **可用**：**相同类型对应相同数据**（统一指标模型）+ 统一维度体系，业务按需取数
3. **可查**：测试期直接在 PG/TimescaleDB 时序库核对原始与 T+0/T+1 数据

---

## 2. 总体架构（标准化 → 数仓 → TSDB → T+0/T+1 → 框架复算）

```
                 ┌──────────────────────────────────────────────────────┐
                 │  IoT 传感器 / 设备（异构采集）                          │
                 │  逆变器 / 储能柜 / 电表 / 红外 / 遥感遥测 ...            │
                 └───────────────────────┬──────────────────────────────┘
                                         │ MQTT (telemetry / up)
                                         ▼
                 ┌──────────────────────────────────────────────────────┐
                 │  ① EMQX（连接 / 会话 / QoS 重传 / 遗嘱）                │
                 └───────────────────────┬──────────────────────────────┘
                                         │ 订阅 topic → 反序列化
                                         ▼
                 ┌──────────────────────────────────────────────────────┐
                 │  ② 标准化层（核心前提：相同类型 → 相同数据）              │
                 │  协议归一 + 单位归一 + 时区归一 + 指标映射(映射表驱动)    │
                 │  红外/遥测/Modbus 的"温度" → 统一 metric_key=temperature │
                 └───────────────────────┬──────────────────────────────┘
                                         │ 统一指标记录
                                         ▼
                 ┌──────────────────────────────────────────────────────┐
                 │  ③ Kafka（削峰 / 解耦 / 多消费复用）                     │
                 │  topic: secp-rich-*-event-data（统一遥测事件）           │
                 └───────────────────────┬──────────────────────────────┘
                                         │ 数仓消费
                                         ▼
   ┌───────────────────────────────────────────────────────────────────────────┐
   │                      ④ 数仓处理层（清洗 + 计算，本方案重点）                  │
   │  L0 接入校验 → L1 标准化 → L2 去重 → L3 质量清洗 → L4 维度补全(场站-系统-租户-设备) │
   │                              │                                            │
   │        ┌─────────────────────┴─────────────────────┐                     │
   │        ▼ T+0 实时计算                                ▼ T+1 经 T+0 再次计算   │
   │   落 TSDB _t0 超表(实时)                       落 TSDB _t1 超表(重算收敛)   │
   └───────────────────────────────┬───────────────────────────────────────────┘
                                    │ 干净的统一指标 + 维度
                                    ▼
                 ┌──────────────────────────────────────────────────────┐
                 │  ⑤ TSDB（PostgreSQL + TimescaleDB 超表）               │
                 │  raw(原始) / dwd(明细) / dws(聚合) / t0(实时) / t1(重算) │
                 └───────────────────────┬──────────────────────────────┘
                                         │ 数仓统一查询接口
                       ┌─────────────────┴──────────────────┐
                       ▼ 业务直调数仓接口（旁路）             ▼
                 ┌──────────────────────────────┐  ┌──────────────────────────────────┐
                 │ ⑥ 框架层（也要计算！复算同口径） │  │ 业务直调数仓接口（监控/结算高频取数）│
                 │  复用数仓计算逻辑，按平台维度聚合 │  └──────────────────────────────────┘
                 └──────────────────┬───────────────┘
                                    ▼
                 ┌──────────────────────────────────────────────────────┐
                 │  ⑦ 平台 / 业务应用                                          │
                 │  监控大屏 / 报表 / 电费结算 / 告警中心 / 碳资产分析            │
                 └──────────────────────────────────────────────────────┘
```

**关键边界（与你确认的架构一致）**：
- **数仓** = 标准化 + 清洗 + 计算 + 时序落库的中心；数仓后端是 **TSDB（TimescaleDB）**，测试即查此处
- **T+0 / T+1 都在 TSDB**：T+0 实时算，T+1 通过 T+0 再次计算（重算收敛迟到/重复/补录）
- **标准化是前置前提**：没有标准化，"相同类型同数据"就无法成立，下游聚合会乱
- **框架也要做计算**：框架不是只封装，而是复用数仓计算口径，在平台侧按业务维度再算一遍（见 §6）
- **业务可直调数仓接口**：高频/简单取数（监控、结算）直接打数仓接口，绕过框架

---

## 3. 标准化设计（核心：相同类型 → 相同数据）

> 这是整条管道的**地基**。不同采集方式（红外、遥感遥测、Modbus 寄存器）表达同一物理含义，必须在数仓入口被归一成**同一个 metric_key**，否则下游无法按"类型"聚合。

### 3.1 原则
**相同类型对应相同数据**：无论数据来自红外探头、遥测通道还是 Modbus 寄存器，只要物理含义相同，落到数仓就是同一个指标、同一单位、同一语义。

### 3.2 例子（红外 / 遥感遥测 / Modbus 归一）
| 采集方式 | 原始表达 | 归一后 |
|---------|---------|--------|
| 红外温度传感器 | `ir_temp=36.5`（℃） | `metric_key=temperature, unit=℃, value=36.5` |
| 遥感遥测温度 | `telemetry.T=309.65`（K） | `metric_key=temperature, unit=℃, value=36.5`（K→℃换算） |
| Modbus 寄存器温度 | `reg[0x10]=365`（0.1℃ 定点） | `metric_key=temperature, unit=℃, value=36.5`（位域/缩放归一） |
| 逆变器有功功率 | `P=3.52`（kW） | `metric_key=active_power, unit=W, value=3520` |
| 电表日发电量 | `E_day=12.3`（kWh） | `metric_key=daily_energy, unit=Wh, value=12300` |

→ 三种"温度"进数仓后是**同一条 metric_key `temperature`**，业务查温度直接 `WHERE metric_key='temperature'`，不用关心来源。

### 3.3 实现：映射表驱动（加设备不改代码）
```yaml
# metric_mapping（配置中心管理，不写死在代码）
- device_type: INVERTER_GW5000
  points:
    - raw: P            # 型号原始字段
      metric: active_power
      unit: kW -> W      # 单位换算
      scale: 1000
    - raw: T            # 寄存器温度
      metric: temperature
      unit: "0.1C" -> "C"
      scale: 0.1
- device_type: IR_SENSOR_X
  points:
    - raw: ir_temp
      metric: temperature   # 与逆变器温度归一到同一指标
      unit: "C" -> "C"
      scale: 1
- device_type: RS_TELEMETRY_Y
  points:
    - raw: T
      metric: temperature
      unit: "K" -> "C"      # 遥感遥测用开尔文，换算成摄氏
      offset: -273.15
```
- 新增型号/传感器：只加一条映射配置，**代码零改动**
- 协议归一、单位归一、时区归一全在标准化层完成，是 L1 的核心输入

### 3.4 为什么标准化必须在数仓入口（而非只在设备端）
- 设备端（边缘）可做第一道换算（单位/位域），但**统一指标映射依赖中心映射表**（型号→指标的关系在平台维护）
- 多协议（红外/遥测/Modbus）的归一规则会演进，集中在数仓层改配置即可，不用刷设备固件
- 故标准化采用"**边缘就近换算 + 数仓集中归一**"两段式

---

## 4. 数据清洗分层设计（分层就近）

清洗在数仓处理层内完成，原则：越靠近接入越先做廉价校验，重计算后置。每层只处理自己职责内的脏数据。

### 4.1 L0 接入校验层
**职责**：拦掉"根本不能要"的数据。

| 规则 | 说明 | 不通过处理 |
|------|------|-----------|
| Schema 校验 | payload 必填字段存在（deviceId、ts、至少一项指标） | 丢弃，记接入失败日志 |
| 设备合法性 | deviceId 在设备白名单/注册表 | 丢弃 + 告警（疑似伪造/未注册） |
| 时间戳合理性 | ts 不超未来（≤ now+5min）、不过旧 | 未来丢弃；过旧进"迟到"通道 |
| 类型可解析 | 数值可转 double，单位在枚举内 | 转换失败丢弃 |

### 4.2 L1 标准化层
**职责**：不同型号/协议/时区/单位 → 统一指标模型（见 §3）。
- 时区对齐：设备本地时间 → UTC
- 单位归一：kW→W、kWh→Wh、K→℃（按映射表）
- 协议归一：型号原始字段 → 统一 `metric_key`
- 批量归一：一次上报多点位拆成多条统一指标记录

### 4.3 L2 去重层
**职责**：保证"同设备同指标同时间"只落一条。

| 策略 | 说明 |
|------|------|
| 传输去重 | QoS1 重传产生相同 `messageId`/`kafkaUuid`，布隆过滤或 Redis 短窗口去重 |
| 业务主键去重 | 落库主键 `(device_id, metric_key, ts)`，UPSERT / `ON CONFLICT DO NOTHING` 物理挡重 |
| 窗口去重 | 同设备 5min 内重复同值，保留首条（可配） |

### 4.4 L3 质量清洗层
**职责**：处理数值本身不合理的数据。

| 规则 | 处理 |
|------|------|
| 越界过滤 | 功率<0 或>额定×1.2；电压超量程 → `quality=SUSPECT`，可配丢弃/限值夹取 |
| 突变检测 | |Δvalue| 相对上周期 > 阈值 → 标记 `SUSPECT` |
| 恒定异常 | 连续 N 点完全相同（应波动）→ 标记 `SUSPECT`（疑似卡死） |
| 缺失检测 | 预期 5min 一点超时未到 → 写 `quality=MISSING` 占位，交 T+1/补录 |

### 4.5 L4 维度补全与衍生层（场站-系统-租户-设备）
**职责**：把"裸点位"补全成"带业务维度的指标"。

| 动作 | 说明 |
|------|------|
| 维度关联 | `deviceId` → 关联出 **stationId（场站）/ systemId（系统）/ tenantId（租户）/ model** |
| 标签注入 | 写入统一维度标签，支撑按层级聚合（设备→场站→系统→租户 rollup） |
| 衍生指标 | 日发电量累计、等效利用小时、充放电效率 |
| 质量透传 | 上游 `quality` 透传，下游区分原值/修复值 |

### 4.6 L5 落库层（时序写入 TSDB）
- 实时写 `raw` / `dwd` 超表（带 quality）
- 实时计算写 T+0 超表（见 §5）
- **攒批 + UPSERT**，主键幂等保证重试安全

---

## 5. T+0 / T+1 计算模型（核心：T+1 通过 T+0 再次计算）

### 5.1 定义
- **T+0（实时）**：设备上报即算，落 TSDB `_t0` 超表。延迟秒级，支撑监控大屏、实时曲线。
- **T+1（离线重算）**：**通过 T+0 再次计算**——拿 `_t0` 已标准化+实时聚合的产物，按天/周期重算，修正迟到数据、重复数据、补录缺口，落 `_t1` 超表。用于报表、结算、对账。

### 5.2 为什么 T+1 从 T+0 算，而不是从原始重算
- T+0 已经是"标准化 + 实时聚合 + 去重"后的干净产物，口径与实时一致
- T+1 在其上重算，只做"收敛"（迟到到达、补录回填、边界修正），**不重复跑标准化/清洗**，计算量小、口径稳
- 两者同源同口径，差异只来自迟到/补录，由 T+1 收敛——这是"实时+离线最终一致"的标准做法

### 5.3 超表落地（TimescaleDB）
```sql
-- T+0 实时（电站光伏有功功率 5min 曲线，与 SECP 真实表对应）
CREATE TABLE algorithm.station_pv_power_5min_t0 (
  station_id   TEXT    NOT NULL,
  date_time    TIMESTAMPTZ NOT NULL,
  p            DOUBLE PRECISION,        -- 实时功率
  quality      TEXT    DEFAULT 'GOOD',
  PRIMARY KEY (station_id, date_time)
);
SELECT create_hypertable('algorithm.station_pv_power_5min_t0','date_time',
       chunk_time_interval => INTERVAL '1 day');

-- T+1 重算（通过 T+0 再次计算，收敛迟到/重复/补录）
CREATE TABLE algorithm.station_pv_power_5min_t1 (
  station_id   TEXT    NOT NULL,
  date_time    TIMESTAMPTZ NOT NULL,
  p            DOUBLE PRECISION,
  quality      TEXT,
  makeup_flg   TEXT,   -- 原值 / 补录值(COPY/AVG/PERCENT/AI)
  PRIMARY KEY (station_id, date_time)
);
SELECT create_hypertable('algorithm.station_pv_power_5min_t1','date_time',
       chunk_time_interval => INTERVAL '1 day');
-- TTL 2 年：ALTER TABLE ... SET (timescaledb.compress, ...);
```
- 同结构还有 `station_load_power_5min_t0/t1`、`station_env_tag_5min_t0/t1`（环测）
- 按天 chunk：查询带时间条件即分区裁剪，只扫相关 chunk
- 主键 `(station_id, date_time)`：覆盖"某场站某时间段"主查询

### 5.4 T+0 / T+1 与维度层级结合
- T+0/T+1 表以 **场站(station)** 为聚合粒度落库
- 更高层（系统/租户）的聚合由 §6 框架层按维度 rollup 复算，不在数仓重算一遍全层级——**数仓算场站级，框架算系统/租户级**，分工清晰

---

## 6. 维度体系：场站 - 系统 - 租户 - 设备

### 6.1 四级层级
统一标签体系包含四个层级（你指定的顺序）：**场站 → 系统 → 租户 → 设备**。
- **设备（device）**：最细粒度，数据产生的叶子节点（逆变器、电表、红外、遥测探头）
- **系统（system）**：一组设备组成的发电/储能单元（如一个光伏子系统、一个储能柜组）
- **场站（station）**：物理电站，含多个系统
- **租户（tenant）**：运营归属主体（客户/业主），场站归其所有

> 聚合 rollup 方向：设备 → 系统 → 场站 → 租户（叶子到根）。任一指标都能沿这条链向上汇总。

### 6.2 维度在数仓层补全、在框架层复用
- 数仓 L4 用 `deviceId` 关联维表补全 `stationId/systemId/tenantId`，写入统一标签
- 数仓统一接口支持按任意维度过滤/聚合（`?stationId=ST001&tenantId=TN01`）
- 框架层按"场站-系统-租户-设备"做业务聚合（见 §7）

---

## 7. 框架层设计（框架也要做计算，复算同口径）

> **重要修正**：框架不是只"封装数仓接口"，**框架也要做计算**——它复用数仓的计算口径，在平台侧按业务维度（场站-系统-租户-设备）再算一遍，输出业务主题指标。

### 7.1 为什么框架要复算
- 数仓算的是**场站级 T+0/T+1 基础指标**（功率、发电量）
- 业务要的是**跨维度汇总 + 业务衍生**：某租户下所有场站总发电量、某系统收益、碳减排
- 这些不能每次都查数仓从原始重算（慢、口径易漂移），框架**复用数仓计算逻辑**（同一套聚合函数/公式），按平台维度 rollup

### 7.2 框架计算 = "通过数仓那样计算"
- 框架内置与数仓**同口径的聚合/计算公式**（如发电量 SUM、收益=发电量×电价）
- 数据源：数仓 T+0/T+1 接口（不直接碰原始表），保证口径一致
- 维度：按"场站-系统-租户-设备"在框架内 rollup

### 7.3 领域计算服务
| 服务 | 框架计算内容 | 数据来源 |
|------|------------|---------|
| 发电曲线服务 | 单/多设备、场站、租户功率曲线 | 数仓 T+0/T+1 接口 |
| 收益服务 | 发电量 × 电价 → 收益（按租户/场站汇总） | 数仓 + 电价维表 |
| 告警服务 | 阈值判定、异常推送 | 数仓 `SUSPECT` 标记 + 规则 |
| 结算服务 | 周期发电量对账（T+1 口径） | 数仓 `_t1` 聚合 |
| 碳资产服务 | 发电量 → 碳减排（按租户汇总） | 数仓衍生指标 |

- **缓存**：热点场站/租户近期指标进 Redis，降低数仓压力
- **降级**：数仓不可用时返回最近缓存，监控不空

### 7.4 业务直调数仓接口（旁路）
- 高频/简单取数（监控实时曲线、结算取数）**直接调数仓统一接口**，绕过框架，降延迟
- 复杂跨域业务计算走框架（复用计算口径）
- 两者并存 = "薄接口（数仓原子查询）+ 厚框架（领域复算）"

---

## 8. 数仓接口层设计

数仓对外暴露**统一查询 API**，框架与业务直调都走它：
```
GET /api/dw/metrics
  ?deviceId=SN123|stationId=ST001|systemId=SY01|tenantId=TN01
  &metric=active_power
  &from=...&to=...
  &agg=raw|5m|1h|t0|t1     # 原始 / 聚合 / 实时 / 重算
  &tags=region:JS
```
- **统一维度过滤**：按场站/系统/租户/设备任意层级
- **聚合下推**：`agg=5m` 查 `dws`，`agg=t1` 查 `_t1`，不回源 raw
- **T+0/T+1 双口径**：业务自选实时还是离线重算结果

---

## 9. 业务层消费

| 业务 | 取数路径 | 说明 |
|------|---------|------|
| 监控大屏 | 直调数仓接口（T+0 实时曲线） | 高频、简单，走旁路 |
| 报表 | 框架层 / 数仓 `_t1` | 日月年聚合（T+1 口径） |
| 电费结算 | 框架层收益服务（复算） | 发电量 × 电价，按租户汇总 |
| 告警中心 | 框架层告警服务 | 消费 quality 标记 |
| 碳资产 | 框架层（复算） | 发电量 → 碳减排，按租户 |

---

## 10. 测试与数据核对

> **约定**：测试期数据直接在 **PG/TimescaleDB 时序库（tsdb 库）**核对，不依赖上层接口。

**测试流程**：
1. 造数：MQTT 模拟器 / 直写 Kafka 注入测试点位（含红外、遥测、Modbus 多类型）
2. 验标准化：查 `raw`，确认不同来源的同物理量已归一为同一 `metric_key`
3. 等落库（实时 `_t0` + 定时 `_t1`）
4. **直接查 PG 时序库核对**：
```sql
-- ① 标准化核对：红外/遥测/Modbus 的温度是否归一到同一 metric
SELECT device_id, metric_key, unit, ts, value
FROM dw.metrics_raw
WHERE metric_key='temperature' AND ts >= '2026-08-28 00:00'::timestamptz
ORDER BY ts;

-- ② T+0 实时曲线
SELECT station_id, date_time, p
FROM algorithm.station_pv_power_5min_t0
WHERE station_id='ST001' AND date_time >= '2026-08-28 00:00'::timestamptz
ORDER BY date_time;

-- ③ T+1 重算结果（通过 T+0 再次计算，含补录标记）
SELECT station_id, date_time, p, makeup_flg
FROM algorithm.station_pv_power_5min_t1
WHERE station_id='ST001' AND date_time >= '2026-08-27 00:00'::timestamptz
ORDER BY date_time;

-- ④ 维度核对：设备 → 场站 → 租户 关联是否补全
SELECT device_id, station_id, system_id, tenant_id
FROM dw.metrics_raw WHERE device_id='SN123' LIMIT 1;
```
5. 校验项：标准化是否同 metric、去重是否唯一、越界是否标 SUSPECT、T+0/T+1 是否一致、维度是否补全、缺失是否 MISSING 占位

---

## 11. 数据质量监控

| 指标 | 口径 | 告警阈值 |
|------|------|---------|
| 接入成功率 | 成功接入 / 总上报 | < 99% |
| 标准化失败率 | 映射失败 / 接入总量 | 突变升高（可能新型号未配映射） |
| 清洗丢弃率 | 各层丢弃 / 接入总量 | 超基线 |
| 去重率 | 重复条数 / 总量 | 异常升高（重传故障） |
| 迟到率 | 迟到 / 总量 | 持续升高（断网/时钟） |
| 缺失率 | `MISSING` / 预期点位 | 超阈值 |
| 异常率 | `SUSPECT` / 总量 | 单设备突增（硬件故障） |
| T+0/T+1 偏差 | `_t1` 与 `_t0` 差异率 | 超阈值（重算异常） |

---

## 12. 设计权衡与 FAQ

**Q：为什么数仓后端用 PG/TimescaleDB（tsdb 库）而不是 ClickHouse / InfluxDB？**
> 团队测试期习惯直接在 PG 时序库查数（tsdb 库）；且业务数据（设备档案、场站、租户、合同）本就在 PG，生态统一、运维栈复用。TimescaleDB 超表 + 按天 chunk 适配"某场站某时间段"查询，配合压缩/TTL 足够。SECP 另有 prophet/ClickHouse 存告警/消息事件记录（数仓的消息记录侧），但时序 T+0/T+1 落在 TSDB。

**Q：标准化为什么这么重要？不做会怎样？**
> "相同类型对应相同数据"是下游聚合的前提。不做标准化，红外温度、遥测温度、Modbus 温度是三条不同 metric，业务查"全站温度"要 union 三张表且单位不一，口径必乱。映射表驱动让加设备/型号零代码改动。

**Q：T+1 为什么要"通过 T+0 再次计算"，不直接从原始算？**
> T+0 已是标准化+去重+实时聚合的干净产物，口径与实时一致；T+1 在其上只做收敛（迟到/补录/边界），计算量小、口径稳，且实时离线最终一致。从原始重算要再跑一遍标准化清洗，既慢又易漂移。

**Q：框架也要做计算，那数仓和框架算重了？**
> 不重，分工不同：数仓算**场站级基础指标（T+0/T+1）**，框架算**系统/租户级业务汇总与衍生（收益、碳资产）**，且框架**复用数仓同口径公式**、数据源是数仓接口——"通过数仓那样计算"，保证口径一致。数仓是薄接口+基础计算，框架是厚领域复算。

**Q：业务直调数仓接口，框架岂不多余？**
> 不矛盾。简单高频取数直调数仓降延迟；复杂跨域业务计算走框架复用口径。两者并存是"薄接口 + 厚框架"分层。

**Q：没有 Flink，能撑住吗？**
> 本方案清洗/计算以"Kafka 消费 + 定时/流式聚合（T+0）+ T+1 重算 + 主键幂等"实现，不强制 Flink。量增长到需精确一次/复杂窗口时，可平滑引入 Flink 替换"数仓消费+聚合"实现，接口与存储不变——**标准化/清洗/存储稳定，计算引擎可替换**。

---

## 附：清洗与计算分层速查表

| 层 | 动作 | 输入 → 输出 | 不通过处理 |
|----|------|------------|-----------|
| 标准化 | 同类型→同数据 | 多协议/多单位/多时区 → 统一 metric | 映射失败告警 |
| L0 | 接入校验 | raw payload → 合法 payload | 丢弃 + 告警 |
| L1 | 标准化 | 多型号 → 统一指标 | 转换失败丢弃 |
| L2 | 去重 | 含重传 → 唯一 (device,metric,ts) | 物理挡重 |
| L3 | 质量清洗 | 越界/突变/缺失 → 标 quality | 标记/丢弃/限值 |
| L4 | 维度补全 | 裸点位 → 带 场站-系统-租户-设备 标签 | 关联失败标 UNKNOWN |
| L5 | 落库 | 干净指标 → TSDB 超表 | UPSERT 重试安全 |
| T+0 | 实时计算 | 干净指标 → `_t0` 超表 | 重试安全 |
| T+1 | 经 T+0 重算 | `_t0` → `_t1` 超表（收敛） | 补录/重算 |
| 框架 | 业务复算 | 数仓接口 → 系统/租户级指标 | 缓存降级 |
