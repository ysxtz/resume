# 高志强简历驱动面试题全集

> 基于当前优化版简历，覆盖技能清单全部 9 大分类 + 4 段工作经历的所有技术点。每题附关键回答要点，结合简历中的真实业务场景。

---

## 目录

- [第一章 Spring 生态与微服务架构](#第一章-spring-生态与微服务架构)
- [第二章 数据库与存储](#第二章-数据库与存储)
- [第三章 消息队列](#第三章-消息队列)
- [第四章 Redis 与分布式锁](#第四章-redis-与分布式锁)
- [第五章 AI Agent 与 LLM 应用](#第五章-ai-agent-与-llm-应用)
- [第六章 工作流引擎](#第六章-工作流引擎)
- [第七章 设备通信与数据管道（含 MQTT/Netty/Kafka/Flink/ES/ClickHouse 深度追问）](#第七章-设备通信与数据管道)
  - [7.7 MQTT 与物联网架构设计（协议/安全/高可用/可靠性/业务清洗存储）](#77-mqtt-与物联网架构设计)
  - [7.8 TimescaleDB 时序库深度追问（原理/选型/hypertable/连续聚合/压缩/分区裁剪/写入/慢查）](#78-timescaledb-时序库深度追问)
- [第八章 DevOps 与监控](#第八章-devops-与监控)
- [第九章 Java 基础与并发](#第九章-java-基础与并发)
- [第十章 固德威 SECP 业务深度追问](#第十章-固德威-secp-业务深度追问)
- [第十一章 江苏风云业务追问](#第十一章-江苏风云业务追问)
- [第十二章 上海精辰业务追问](#第十二章-上海精辰业务追问)
- [第十三章 南京紫金业务追问](#第十三章-南京紫金业务追问)
- [第十四章 综合场景设计题](#第十四章-综合场景设计题)

---

## 第一章 Spring 生态与微服务架构

### 1.1 Spring Boot

**Q1: 你简历中提到"熟练掌握 Spring Boot"，说说 Spring Boot 的自动装配原理？**

要点：
- `@SpringBootApplication` → `@EnableAutoConfiguration` → `@Import(AutoConfigurationImportSelector.class)`
- 加载 `META-INF/spring.factories`（Spring Boot 2.x）或 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`（2.7+）
- 通过 `@ConditionalOnClass`、`@ConditionalOnMissingBean` 等条件注解决定是否装配
- SECP 中的例子：`goodwe-persistence-starter` 自研 Starter，通过 `spring.factories` 注册 MyBatis-Plus 配置类

**Q2: Spring Boot 启动流程中，Bean 的创建经历了哪些阶段？**

要点：
- 资源加载 → BeanDefinition 注册 → 实例化 → 属性填充 → 初始化（BeanPostProcessor 前置 → InitializingBean → init-method → 后置）→ AOP 代理 → 完整 Bean
- 结合简历：SECP 中的 `BusinessLockAspect` 通过 AOP 在 Bean 初始化后生成代理，`@Aspect` + `@Around` 实现声明式分布式锁

### 1.2 Spring Cloud Alibaba

**Q3: 你们用 Nacos 做服务注册中心，Nacos 和 Eureka 的区别是什么？为什么选 Nacos？**

要点：
- Nacos 支持 AP/CP 切换，Eureka 只有 AP
- Nacos 集成配置中心，Eureka 需要配合 Spring Cloud Config
- Nacos 支持命名空间+分组+Data ID 三级隔离，适合 SECP 30+ 微服务的多环境管理
- Nacos 有权重流量管控，灰度发布更方便

**Q4: Spring Cloud Gateway 在你们项目中怎么用的？路由和限流怎么配置？**

要点：
- Gateway 基于 WebFlux + Reactor，非阻塞式，比 Zuul 性能好
- 路由配置：`spring.cloud.gateway.routes`，通过 Path 断言路由到各微服务
- 限流：结合 Sentinel 或 RequestRateLimiter（Redis + Lua 令牌桶）
- 结合简历：SECP 30+ 微服务通过 Gateway 统一入口，BFF 层（secp-we-app）聚合多服务数据

**Q5: OpenFeign 的工作原理？你们怎么传递 Token 的？**

要点：
- OpenFeign 通过 `@FeignClient` 声明接口，运行时通过 JDK 动态代理生成实现类
- 请求拦截器 `RequestInterceptor` 注入 Header（Authorization: Bearer xxx）
- 结合简历：SECP 中 `SecurityInterceptor` 实现 Feign 令牌透传，保证微服务间调用携带用户上下文

**Q6: Sentinel 和 Hystrix 的区别？你们怎么用的？**

要点：
- Sentinel 基于滑动窗口统计，支持流控/熔断/系统自适应；Hystrix 基于计数器，已停止维护
- Sentinel 有控制台实时监控，Hystrix 需要配合 Dashboard
- SECP 中用 Sentinel 做核心接口的限流和熔断降级

### 1.3 微服务架构

**Q7: 你们从单体拆成 30+ 微服务，拆分的原则是什么？**

要点：
- 按业务领域拆分：结算、告警、设备管理、用户权限各自独立服务
- 按变化频率拆分：高频迭代的功能独立部署，减少互相影响
- 按团队边界拆分：对应简历中"400 人、数仓/平台/框架/业务/定制/产品六个组"
- 数据库按服务拆分，避免跨服务 JOIN

**Q8: 微服务拆分后，分布式事务怎么处理的？**

要点：
- 强一致性：Seata AT 模式（少用，性能影响大）
- 最终一致性：RocketMQ 事务消息（简历中结算对账用的就是这种）
- 业务补偿：通过 Flowable 工作流实现人工补偿节点
- 结合简历：结算场景选择事务消息而非本地事务表方案，因为"需要最终一致性且对写入延迟敏感"

**Q9: 服务间怎么做链路追踪的？**

要点：
- SkyWalking 字节码注入，无侵入
- TraceId 通过 ThreadLocal + 请求头传递
- 结合简历：SECP 有自研 `TraceIdFilter`，通过 MDC 注入 TraceId 到日志，跨服务通过 Feign 拦截器传递

**Q10: 灰度发布怎么做的？**

要点：
- Nacos 元数据标记服务版本
- Gateway 路由规则根据 Header/Cookie 路由到灰度实例
- Sentinel 流控规则控制灰度流量比例

---

## 第二章 数据库与存储

### 2.1 PostgreSQL

**Q11: 你们为什么选 PostgreSQL 而不是 MySQL？**

要点：
- JSONB 字段支持复杂结构化数据存储（设备配置、告警规则等半结构化数据）
- 递归 CTE（WITH RECURSIVE）适合组织树、权限树查询（SECP 中设备拓扑树）
- 窗口函数（ROW_NUMBER OVER PARTITION BY）用于时序数据分析
- ON CONFLICT DO UPDATE 实现幂等 upsert（设备数据高频写入场景）
- 对比 MySQL：PostgreSQL 的 MVCC 实现更适合 OLAP+OLTP 混合场景，地理字段（PostGIS）支持好

**Q12: PostgreSQL 的 MVCC 和 MySQL InnoDB 的 MVCC 有什么区别？**

要点：
- PostgreSQL：多版本存储在表中，VACUUM 清理死元组；MySQL：undo log 回滚段
- PostgreSQL 不需要回滚段，但需要定期 VACUUM
- PostgreSQL 的隔离级别默认 Read Committed，MySQL 默认 Repeatable Read
- PostgreSQL 没有 READ UNCOMMITTED（等同 READ COMMITTED）

**Q13: 你提到 PostgreSQL 窗口函数，在业务中怎么用的？**

要点：
- `ROW_NUMBER() OVER (PARTITION BY device_id ORDER BY timestamp DESC)` 取每个设备最新一条数据
- SECP 中 `SolarTheoryEnergyDataDao.xml` 用窗口函数计算每日报发电量排名
- 对比子查询方案，窗口函数性能更好（一次扫描完成）

**Q14: WITH RECURSIVE 递归查询在什么场景用？**

要点：
- SECP 中 `ThingInfoDao.xml` 用递归 CTE 查询设备拓扑树（场站→逆变器→组件）
- 权限树递归：查某角色的所有子权限
- 对比 Java 代码递归：SQL 递归减少数据传输量，一次查询完成多层遍历

**Q15: ON CONFLICT DO UPDATE 你们在什么场景用？**

要点：
- 设备数据高频写入，相同 device_id + timestamp 的数据幂等写入
- SECP 中 `LocationWeatherNowTodayDao.xml` 用 ON CONFLICT 实现天气数据 upsert
- 对比 MySQL 的 INSERT ON DUPLICATE KEY UPDATE

### 2.2 MySQL

**Q16: MySQL 索引的 B+ 树结构，为什么用 B+ 树而不是 B 树或红黑树？**

要点：
- B+ 树非叶子节点不存数据，扇出更大，树更矮，IO 次数少
- 叶子节点有序链表，范围查询效率高
- 对比红黑树：二叉树在百万级数据下树高约 20+，IO 次数过多

**Q17: 你说优化接口从 3s 到 0.2s（南京紫金），具体怎么做的？**

要点：
- 慢 SQL 排查：EXPLAIN 分析执行计划，发现全表扫描
- 索引优化：WHERE 条件字段加联合索引，遵循最左前缀原则
- 查询优化：避免 SELECT *，大表分页用游标分页而非 OFFSET
- JOIN 优化：驱动表选小表，被驱动表 JOIN 字段加索引

### 2.3 Elasticsearch

**Q18: 你们 ES 按月分索引是怎么设计的？为什么不直接用单索引？**

要点：
- 告警数据持续增长，单索引膨胀后查询性能退化
- 按月分索引（`alert_events_202503`）可独立管理生命周期
- 查询时通过索引别名 + 日期通配符路由到最近 6 个月索引
- 老旧索引可以迁移到冷节点或直接删除
- 对比方案：单索引 + rollover，但按月分对业务更直观

**Q19: ES 的倒排索引原理？和 B+ 树索引的区别？**

要点：
- ES 倒排索引：分词 → Term → Posting List（文档ID列表）
- 适合全文检索（告警事件内容搜索）
- B+ 树适合精确等值和范围查询
- ES 用 BM25 评分做相关性排序，B+ 树没有

### 2.4 ClickHouse

**Q20: ES 和 ClickHouse 在你们项目中分别存什么数据？**

要点：
- ES 存告警事件（需要全文检索 + 多维过滤）
- ClickHouse 存设备时序指标数据（需要大范围聚合统计，如月度发电量趋势）
- ES 侧重搜索，ClickHouse 侧重 OLAP 聚合
- ClickHouse 列式存储 + 向量化执行，亿级数据聚合秒级返回

---

## 第三章 消息队列

### 3.1 RocketMQ

**Q21: 你说用 RocketMQ 事务消息做异步对账，事务消息的原理是什么？**

要点：
- 半消息：先发一条对生产者不可见的消息
- 执行本地事务（写对账记录）
- 根据本地事务结果：commit（消息可见）或 rollback（消息删除）
- 如果超时未确认，Broker 回查生产者本地事务状态
- 结合简历：结算对账——先发半消息，执行对账写入，成功后 commit 让下游消费

**Q22: 为什么选 RocketMQ 事务消息而不是本地事务表方案？**

要点（简历中的选型思考）：
- 本地事务表需要额外建表 + 定时扫描 + 重试逻辑，开发成本高
- 事务消息原生支持最终一致性，Broker 自动回查
- 结算对账对写入延迟敏感，事务消息的半消息机制不阻塞业务流程
- RocketMQ 的事务消息在阿里系大规模验证过，可靠性有保障

**Q23: RocketMQ 怎么保证消息不丢失？**

要点：
- 生产端：同步发送 + 重试（非 oneway）
- Broker：同步刷盘 + 主从复制（SYNC_MASTER）
- 消费端：手动 ACK，消费成功后才返回 CONSUME_SUCCESS
- 死信队列：消费失败的消息进入 DLQ，人工介入

### 3.2 Kafka

**Q24: Kafka 在你们告警引擎中怎么用的？**

要点：
- 设备事件通过 MQTT 上报 → Kafka Topic → 消费者匹配规则 → 写入 ES
- Kafka 高吞吐适合千万级设备的事件流
- Consumer Group 实现多实例并发消费，按设备 ID 分区保证顺序

**Q25: Kafka 怎么保证消息有序？**

要点：
- 同一分区内消息有序
- 按设备 ID 做 partition key，保证同一设备的事件路由到同一分区
- 消费者单线程消费同一分区，或用内存队列做二级分区

### 3.3 RabbitMQ

**Q26: 你在上海精辰用 RabbitMQ 做异步处理，RabbitMQ 和 Kafka 有什么区别？怎么选型？**

要点：
- RabbitMQ：延迟低（微秒级），消息可靠性高（确认机制），适合业务消息
- Kafka：吞吐量高（百万级/秒），适合日志和流处理
- 精辰场景：MES 数据插入量不大但需要严格不丢消息，选 RabbitMQ 合适
- SECP 场景：千万级设备事件流，选 Kafka 合适

**Q27: RabbitMQ 的消息确认机制？**

要点：
- 生产者确认（Publisher Confirm）：消息到达 Broker 后回调
- 消费者确认（Consumer ACK）：手动确认后才从队列删除
- 消息回退（Return）：消息无法路由时回退给生产者

---

## 第四章 Redis 与分布式锁

### 4.1 Redis 基础

**Q28: Redis 的常用数据结构你们在业务中怎么用的？**

要点：
- String：Token 存储（TokenRedisDao）、分布式锁
- Hash：设备实时状态缓存
- ZSet：告警排行榜、标签热度排序
- List：消息队列（轻量场景）
- Pub/Sub：ThingListener 监听设备变更通知

**Q29: 缓存穿透、击穿、雪崩分别是什么？你们怎么处理的？**

要点：
- 穿透：查不存在的数据 → 布隆过滤器 / 缓存空值
- 击穿：热点 key 过期 → 互斥锁重建（Redisson lock）
- 雪崩：大量 key 同时过期 → 过期时间加随机值 + 多级缓存
- SECP 中 BFF 优化用了多级缓存（本地 Caffeine + Redis）

### 4.2 Redisson 分布式锁

**Q30: 你简历里说用 Redisson 分布式锁保证结算幂等，Redisson 锁的原理是什么？**

要点：
- 基于 Lua 脚本 + Redis：SET key value NX PX + value 为 UUID
- 看门狗（WatchDog）机制：默认 30s 过期，每 10s 续期，适配长事务场景（简历原话）
- 可重入：通过 Hash 结构记录线程重入次数
- 释放锁：Lua 脚本判断 UUID 匹配后删除，避免误删

**Q31: 为什么选 Redisson 而不是 ZooKeeper 做分布式锁？**

要点（简历选型思考）：
- Redisson 性能更高（Redis 内存操作 vs ZK 磁盘 + 网络广播）
- Redisson 看门狗自动续期，适合结算长事务场景
- ZK 强一致但性能差，SECP 已经有 Redis 基础设施，不需要额外引入 ZK
- Redisson 提供声明式 AOP 注解（`@BusinessLock`），开发成本低

**Q32: Redisson 锁的看门狗机制，如果服务挂了怎么办？**

要点：
- 看门狗线程随 JVM 生命周期，服务挂了不再续期
- 锁会在 30s 后自动过期释放
- 业务侧配合 RocketMQ 事务消息做补偿：锁释放后消息可见，下游可以继续对账

### 4.3 多级缓存

**Q33: 你们 BFF 层的多级缓存怎么设计的？**

要点：
- L1：本地缓存 Caffeine（毫秒级响应，容量有限）
- L2：Redis 分布式缓存（跨实例共享）
- 查询顺序：L1 → L2 → DB
- 更新策略：先更新 DB，删除 Redis，清空本地缓存（通过 Pub/Sub 通知其他实例）
- 结合简历：SECP 场站概览页用多级缓存大幅降低响应时间

---

## 第五章 AI Agent 与 LLM 应用

**Q34: 你说独立开发了运维智能助手 Agent，整体架构是什么？**

要点：
- 用户通过飞书机器人输入告警 ID 或自然语言
- Spring AI 编排 LLM（对接大模型），LangChain4j 管理 Prompt 模板和工具链
- Function Calling：LLM 决定调用哪个内部 API（查设备状态、查指标数据）
- RAG：检索运维知识库（SOP 文档、历史故障案例），注入 Prompt 上下文
- Session 上下文存 Redis，支持多轮对话

**Q35: 为什么选 Spring AI + LangChain4j 而不是直接调 LLM API？**

要点（简历选型思考）：
- 需要 Prompt 模板统一管理（不同告警类型用不同 Prompt）
- 需要工具调用编排（Function Calling 的工具注册和路由）
- 需要会话上下文持久化（Redis Session 管理）
- LangChain4j 提供 RAG 全流程（文档加载→分块→向量化→检索→注入）
- 直接调 API 需要自己实现上述所有功能

**Q36: RAG 的流程是什么？你们知识库怎么建的？**

要点：
- 文档加载：运维 SOP、历史故障案例（PDF/Word/Markdown）
- 分块（Chunking）：按段落/固定 token 数切分
- 向量化：Embedding 模型转为向量
- 存储：向量数据库
- 检索：用户问题向量化 → 相似度搜索 → Top-K 文档块
- 注入：将检索结果拼入 Prompt 上下文

**Q37: Function Calling 怎么实现的？**

要点：
- 定义工具 Schema（函数名、参数、描述）
- 注册到 Spring AI 的 ChatClient
- LLM 根据用户意图选择调用哪个工具
- 框架自动执行工具函数，将结果返回给 LLM
- SECP 中：查设备状态 API、查实时指标 API、查历史告警 API

**Q38: Agent 的多轮对话怎么管理的？**

要点：
- Session ID 存 Redis（key: session_id, value: 对话历史 + 上下文）
- 每轮对话带上历史消息列表发给 LLM
- 设置最大轮数/最大 token 数，超出时做摘要压缩
- 超时自动清理（Redis TTL）

**Q39: Agent 上线后告警诊断从 5 分钟到 30 秒，这个提升主要来自哪里？**

要点：
- 原流程：运维人员收到告警 → 登录系统查设备 → 翻 SOP 文档 → 人工判断
- Agent 流程：告警自动触发 Agent → LLM 分析 + RAG 检索 SOP → 自动给出分类和处置建议
- 主要提升：省去了人工查文档和系统操作的时间
- 日均 2000+ 条告警，TOP 10 高频类型 Agent 自动处理

---

## 第六章 工作流引擎

**Q40: 你们用 Flowable 封装了通用工作流，具体怎么封装的？**

要点：
- 抽象通用 ProcessService：发起流程、审批、驳回、查询待办
- 通用 variables Map 设计：业务流程动态扩展参数，不改流程定义
- 支持 5 类业务流程：运维工单、合同审批、电费结算、巡检计划、零碳审核
- 前端统一审批组件，后端统一审批接口

**Q41: Flowable 和 Activiti 的区别？你两个都用过，为什么后期换 Flowable？**

要点：
- Flowable 是 Activiti 原班人马 fork 的分支
- Flowable 支持 CMMN（案例管理）和 DMN（决策表），Activiti 不支持
- Flowable 性能更好（异步执行器优化）
- Flowable 社区更活跃
- 精辰用 Activiti（历史原因），固德威用 Flowable（新项目选型）

**Q42: Flowable 的流程定义怎么部署和更新？**

要点：
- BPMN 2.0 XML 文件部署到 Flowable 引擎
- 版本管理：每次部署自动 +1，旧实例继续走旧版本
- 动态更新：流程变更后，运行中实例可以迁移到新版本

---

## 第七章 设备通信与数据管道

**Q43: 你们千万级设备数据管道的整体架构是什么？**

要点：
```
设备 → MQTT → Kafka → Flink 流处理 → ES（检索）/ ClickHouse（统计）
```
- MQTT 采集层：设备上报遥测数据
- Kafka 缓冲层：削峰填谷，解耦采集和处理
- Flink 处理层：实时清洗、聚合、规则匹配
- ES/ClickHouse 存储层：ES 做全文检索，ClickHouse 做时序统计

**Q44: MQTT 的 QoS 级别？你们用的哪个？**

要点：
- QoS 0：最多一次，不保证到达（适合高频非关键数据）
- QoS 1：至少一次，可能重复（适合一般业务数据）
- QoS 2：恰好一次，开销最大（适合关键指令）
- SECP 设备遥测数据用 QoS 1（允许少量重复，Flink 去重）

**Q45: gRPC 在你们项目中用在哪？和 HTTP/REST 的区别？**

要点：
- gRPC 用于微服务间高频调用（设备数据查询）
- Protobuf 二进制序列化，比 JSON 体积小 30%+
- HTTP/2 多路复用，减少连接数
- 对比 REST：gRPC 性能好但不便调试，适合内部调用

**Q46: Flink 在数据管道里做什么？**

要点：
- 实时清洗：过滤异常数据（传感器故障产生的脏数据）
- 窗口聚合：1 分钟/5 分钟滚动窗口聚合设备功率
- 规则匹配：设备指标超阈值生成告警事件
- 写入 ES 和 ClickHouse

### 7.2 MQTT + Netty 接入层

**Q46-1: MQTT 的 Topic 你们怎么设计的？为什么这么分？**

要点：
- 按设备类型 + 设备ID 分层：`secp/{deviceType}/{deviceId}/telemetry`（遥测）、`secp/{deviceType}/{deviceId}/event`（事件）
- 设备ID 作为 Topic 一级，方便按设备订阅和权限控制
- 不用通配符 `#` 订阅全量消息（消息量太大，消费端扛不住），而是按设备类型订阅 `secp/inverter/+/telemetry`
- 遗嘱消息（Will Message）：设备上线时注册遗嘱 Topic `secp/{deviceId}/offline`，设备异常断连时 Broker 自动发布，消费端感知设备离线

**Q46-2: 百万级 MQTT 长连接怎么支撑的？用的什么 Broker？**

要点：
- EMQX 作为 MQTT Broker，集群部署，单节点支撑 100 万+ 并发连接
- Netty NIO Reactor 模型：BossGroup 接受连接，WorkerGroup 处理 IO，一个线程管理数千连接，不是一连接一线程
- 连接保活：MQTT Keep-Alive 心跳机制，默认 60s，设备每 60s 发 PINGREQ，Broker 回 PINGRESP
- 连接状态存储：EMQX 将 Session 信息存到 Redis/MySQL，Broker 节点宕机后其他节点可恢复 Session
- SECP 实际场景：全球设备分布在不同时区，连接数白天高夜间低，按地域部署 EMQX 集群分摊负载

**Q46-3: Netty 的 Reactor 线程模型说一下？你们 MQTT 接入用的是什么模型？**

要点：
- 主从 Reactor 模型：BossGroup（1个线程）接受连接 → 把连接分配给 WorkerGroup（N个线程）处理 IO
- WorkerGroup 的 EventLoop 绑定 Selector，一个 EventLoop 管理多个 Channel
- ChannelPipeline 责任链模式：编解码器 → 业务 Handler 依次处理
- SECP MQTT 接入：用 Netty 自定义 MQTT 编解码器（MQTTMessageEncoder/Decoder），业务 Handler 处理 CONNECT/PUBLISH/SUBSCRIBE 报文
- 注意：业务 Handler 不能在 EventLoop 线程做耗时操作（会阻塞 IO），耗时操作丢到业务线程池

**Q46-4: MQTT 和 HTTP 相比，为什么 IoT 场景选 MQTT？**

要点：
- 协议轻量：MQTT 固定头部仅 2 字节，HTTP 头部动辄几百字节，设备带宽有限
- 长连接：MQTT 一条 TCP 连接持续通信，HTTP 每次请求都建连接（HTTP 长连接也有但不如 MQTT 原生）
- 发布订阅模型：设备只管发布，Broker 分发给多个订阅者，解耦生产者和消费者
- QoS 保障：HTTP 没有 QoS 概念，MQTT 有 0/1/2 三个级别
- 遗嘱机制：设备异常断连 Broker 自动通知，HTTP 做不到

**Q46-5: 设备离线了怎么感知？消息怎么处理？**

要点：
- 感知离线：MQTT 遗嘱消息（Will Message）+ Keep-Alive 超时检测
  - 设备连接时注册遗嘱 Topic，断连后 Broker 自动发布遗嘱消息
  - Keep-Alive 超时：设备 1.5 倍心跳周期没发 PINGREQ，Broker 判定断连
- 离线消息处理：
  - QoS 1/2 + Clean Session=false：Broker 保留 Session，设备重连后补发离线期间的消息
  - 遥测数据（QoS 1）：离线期间的数据设备本地缓存，重连后批量上报
  - 关键指令（QoS 2）：Broker 存储离线消息，设备重连后投递

### 7.3 Kafka 缓冲层

**Q46-6: Kafka 的 Topic 和 Partition 你们怎么设计的？**

要点：
- 按设备类型分 Topic：`secp-inverter-telemetry`、`secp-battery-telemetry`、`secp-alarm-event`
- Partition 数 = 预期峰值吞吐 / 单分区吞吐量（单分区约 10MB/s）
  - SECP 设备遥测 Topic：24 个 Partition（峰值约 200MB/s）
  - 告警事件 Topic：6 个 Partition（数据量小）
- 分区策略：按设备ID hash，保证同一设备的消息落到同一分区，消费有序
- Partition 数 = 消费者数，一个消费者消费一个分区，并行度最大化

**Q46-7: Kafka 消费者组的 Rebalance 机制说一下？你们遇到过 Rebalance 风暴吗？**

要点：
- Rebalance 触发条件：消费者加入/离开消费组、订阅 Topic 的 Partition 数变化
- 过程：Coordinator 选出 Leader 消费者 → Leader 分配分区方案 → 下发给所有消费者
- Rebalance 期间所有消费者停止消费（STW），频繁 Rebalance 会导致消费延迟
- Rebalance 风暴场景：消费者 GC 停顿超过 session.timeout.ms（默认 10s）→ 被踢出消费组 → Rebalance → 恢复后又加入 → 再次 Rebalance
- SECP 调优：
  - `session.timeout.ms=30000`（30s，避免 GC 停顿误判）
  - `heartbeat.interval.ms=10000`（10s 心跳，在 session 超时前发 3 次心跳）
  - `max.poll.interval.ms=300000`（5分钟，避免处理慢被踢）
- GC 停顿和 Kafka Rebalance 的关联：G1 MaxGCPauseMillis=200ms 保证 GC 停顿远小于 session 超时

**Q46-8: Kafka 怎么保证消息不丢失？生产端和消费端分别怎么做？**

要点：
- 生产端：
  - `acks=all`：消息写入 Leader + 所有 ISR 副本后才返回成功
  - `retries=3`：发送失败自动重试
  - `enable.idempotence=true`：幂等生产者，重试不产生重复消息（PID + SequenceNumber）
- Broker 端：
  - `min.insync.replicas=2`：至少 2 个副本同步成功才算写入成功
  - `replication.factor=3`：3 副本
  - `unclean.leader.election.enable=false`：禁止非 ISR 副本当选 Leader（防数据丢失）
- 消费端：
  - 手动提交 offset：处理完消息后再 commit，不用自动提交（auto.commit=false）
  - 处理失败的消息进入死信队列（DLT），不阻塞正常消费

**Q46-9: Kafka 的 offset 你们怎么管理的？自动提交还是手动提交？**

要点：
- 手动提交（`enable.auto.commit=false`）
  - 原因：自动提交每 5s 提交一次，如果消费了消息但还没处理完就提交了 offset，宕机会丢消息
  - 手动提交：消息处理完再 commit，保证 At-Least-Once 语义
- 提交方式：`commitSync()`（同步提交，阻塞，但可靠）vs `commitAsync()`（异步提交，不阻塞，但可能失败）
- SECP 做法：正常消费用 commitAsync，消费者关闭时用 commitSync 确保最后一批 offset 提交成功
- offset 存储在 Kafka 内部 Topic `__consumer_offsets`（早期版本存 ZooKeeper）

**Q46-10: Kafka 和 RocketMQ 你们都用了，架构上有什么区别？为什么告警用 Kafka、结算用 RocketMQ？**

要点：
- 架构区别：
  - Kafka：Partition 为基本单位，每个 Partition 独立，无 Queue 概念，消费严格按 Partition 有序
  - RocketMQ：Topic 下分 Queue，支持消息Tag过滤、延迟消息、事务消息
- 选型逻辑：
  - 告警用 Kafka：设备事件量大、吞吐要求高、不需要事务消息，Kafka 吞吐能力更强
  - 结算用 RocketMQ：需要事务消息保证最终一致性（半消息 → 本地事务 → commit/rollback），需要延迟消息做对账触发，RocketMQ 原生支持

**Q46-11: Kafka 消费者积压了怎么排查和处理？**

要点：
- 排查：
  - `kafka-consumer-groups.sh` 查看 Lag（滞后量 = LogEndOffset - ConsumerOffset）
  - SkyWalking 链路追踪看消费端哪个环节慢
  - 看 GC 日志，是否有长时间 STW 导致消费暂停
- 处理：
  - 临时：增加消费者实例（但不超过 Partition 数，多了会空闲）
  - 永久：增加 Partition 数 + 消费者数
  - 如果是处理慢：优化消费逻辑，比如 ES 批量写入代替逐条写入
  - 如果是突发流量：Kafka 本身就是削峰填谷的，消费端按最大能力消费即可，积压会在流量回落后追平

### 7.4 实时处理层（⚠️ SECP 真实实现：Spring Kafka + 定时调度，不是 Flink）

> **代码核实更正（2026-08-28，G:\Project\goodwe）**：简历/旧稿写"Kafka → Flink → 落库"，但**全仓库检索不到 `org.apache.flink`，Flink 在 SECP 中不存在**。真实实现是 **Spring Kafka**（`@KafkaListener` + `ConcurrentKafkaListenerContainerFactory`，代码恰好放在 `biz/message/flink` 包下，包名有误导性）+ **定时调度**（`@Async` / `@Scheduled`）。设备曲线落库是 `secp-algorithm` 的 `StationMetricScheduleServiceImpl` 定时从 watchman 拉数据写 PG/TimescaleDB，并非 Flink 作业。下面先给 SECP 真实 Q&A，再把 Flink 通用原理列为"知识储备"（面试可讲，但坦诚本项目不用）。

**Q46-12: SECP 设备数据（含数仓数据）是怎么从 Kafka 消费的？**

要点：
- 接入/转发层（secp-iot-*/message-convert/middle-forward，源码在本地 checkout 之外）把设备数据送入 Kafka
- 数仓侧：`secp-prophet` 的 `DW*DataSource`（`DWEventDataSource` / `DWStatusMessageDataSource` / `DWRunningStatusMessageDataSource` 等）用 `@KafkaListener` 消费 `spring.kafka.data-warehouse-kafka.*`（topic 形如 `secp-rich-*-event-data`），转 `UnifiedMessage` 后写 ClickHouse（这才是"设备数据进数仓"的真实落点）
- 业务消息侧：`secp-manager` 的 `biz/message/flink` 包下 `AbstractKafkaDataSource` 子类消费设备-产品关系、用户、操作日志等
- 技术栈：`ConcurrentKafkaListenerContainerFactory` 并发消费 + 手动 ack，处理失败不提交 offset 可重放

**Q46-13: 数据清洗和去重在 SECP 怎么做的？（分层就近）**

要点：
- 边缘侧（Rust）：`formula` 引擎把裸 Modbus 寄存器换算成物理量（单位/位域/缩放），上行已是干净物理量
- 数仓侧（prophet）：`DW*DataSource.convert()` 反序列化 → 过滤 `status` 为 null 的脏数据 → SN 布隆过滤挡未知设备 → `AbstractMessageParser` 插件链做字段关联补全 + 正则校验 → 写 ES 时主键幂等去重（重复消息写失败丢弃）+ ES 失败消息剔除
- 时序侧（algorithm）：5min 聚合 + Redis 记上次执行时间做调度幂等 + T+1 重算收敛迟到数据
- data-config：缺失检测（`DwMissingDataInfo`）+ 四种补录策略（`MakeupRuleEnum`：COPY / AVG / PERCENT / AI）+ 异常标签（`InconsistentTagEnum`）
- 详见《数据清洗_链路实现详解.md》《数据清洗方案设计文档.md》

**Q46-14: 5 分钟功率/发电量曲线是怎么生成的？**

要点：
- `secp-algorithm` 的 `StationMetricScheduleServiceImpl.generateRt5MinCurves()`（`@Async` 调度）定时触发
- 调 `watchmanService.getStationMetricData(...)` 拉取设备实时数据 → 聚合（SUM/AVG）→ 批量 `saveBatch` 写 PG/TimescaleDB 超表 `station_pv_power_5min_t0`（T+0）与 `_t1`（T+1）
- T+0 给实时监控大屏用；T+1 定时重算修正迟到/重复，给结算报表用
- 主键 `(station_id, date_time)` 天然幂等，重试安全

**Q46-15: 千万级设备怎么保证不丢数据、不重复？**

要点（端到端）：
- 接入：MQTT QoS1 + Broker 会话保持（断线补投）
- 缓冲：Kafka 多副本（acks=all + min.insync.replicas）+ 保留期可重放
- 消费：Spring Kafka 手动 ack，处理失败不提交 offset
- 落库：PG/TimescaleDB 主键 `(station_id/device_id, ts)` + UPSERT 幂等；ClickHouse 侧 ES 主键去重
- 收敛：T+1 重算覆盖迟到/重复，最终一致

**Q46-16: 为什么你们没用 Flink？（坦诚回答）+ Flink 通用知识储备**

要点：
- 本项目数据规模与计算复杂度，用 Spring Kafka 消费 + 定时调度聚合已能满足（实时性为分钟级曲线，非毫秒级流计算），引入 Flink 增加运维/开发成本
- 清洗是"分层就近"而非单一流作业，去重靠主键幂等、聚合靠定时拉取，不需要 Flink 的 Exactly-Once / Checkpoint
- 若面试被问到流处理通用原理，可讲下面"知识储备"，但需说明 SECP 未落地 Flink：

**【知识储备】Flink 通用原理（面试可讲，SECP 未使用）**
- Checkpoint：JobManager 注入 Barrier → 算子对齐状态快照到外部存储，失败后从最近 Checkpoint 恢复，保证 Exactly-Once
- Watermark：事件时间水印 = maxEventTime - 允许延迟，处理乱序；超延迟数据进 side output
- 窗口：滚动（Tumbling）/ 滑动（Sliding）/ 会话（Session）；设备功率聚合用滚动窗口，告警匹配用 KeyedProcessFunction
- 状态后端：RocksDBStateBackend（大状态落盘，Checkpoint 快照到外部存储），每设备维护 ValueState 判功率骤降
- Exactly-Once：Flink + Kafka 两阶段提交（Checkpoint 提交 offset）；ES Sink 不支持事务，用 `_id` 幂等写
- 反压：下游慢致缓冲区堆积；调 Bulk 批量、buffer-timeout、JVM(G1) 调优

### 7.5 Elasticsearch 存储层

**Q46-18: ES 按月分索引的具体方案是什么？查询跨月怎么办？**

要点：
- 索引命名：`secp_alarm_event_202601`、`secp_alarm_event_202602`
- 创建：每月初通过 Index Lifecycle Management (ILM) 自动创建新索引，或定时任务创建
- 查询跨月：用 Index Alias + 通配符查询
  ```
  GET secp_alarm_event_2026*/_search  -- 查 2026 年全年
  GET secp_alarm_event_*/_search       -- 查所有（慎用）
  ```
- 优势：
  - 单索引数据量可控（每月约几千万条），查询性能稳定
  - 可以按月删除历史数据（ILM 自动 delete 6 个月前的索引），不用 reindex
  - 每月索引可以独立调整分片数、副本数、mapping
- SECP 实际：保留 6 个月滚动查询窗口，超过 6 个月的索引自动归档到 ClickHouse

**Q46-19: ES 的分片（Shard）你们怎么设置？分片太多或太少有什么问题？**

要点：
- 分片数设置原则：单分片大小控制在 30-50GB
  - SECP 月度索引约 5GB，设 2 个主分片 + 1 副本
- 分片太多的问题：
  - 每个分片是一个 Lucene 实例，占用内存和文件句柄
  - 搜索时需要 fan-out 到所有分片再合并结果，分片太多增加协调节点开销
  - 集群状态变大（Master 节点维护分片元数据），影响集群稳定性
- 分片太少的问题：
  - 单分片数据量过大，查询慢
  - 无法水平扩展（分片数创建后不可变，只能 reindex）
- 副本数：生产环境至少 1 副本，保证节点宕机不丢数据

**Q46-20: ES 的写入性能怎么优化？你们告警数据写入做了哪些调优？**

要点：
- 批量写入：Bulk API，每批次 1000-5000 条，减少 HTTP 请求次数
- refresh_interval 调大：默认 1s 刷新一次（新数据可搜索），写入密集期改为 30s 或 -1（关闭自动刷新），写入完手动 refresh
- 副本数暂时设 0：大量写入时临时关闭副本，写完再恢复（减少副本同步开销）
- 禁用 _all 字段和动态映射：Mapping 明确定义字段类型，避免自动推断开销
- ES 写入流程：document → indexing buffer → segment（refresh）→ disk（flush）
- SECP 告警写入：Kafka 消费者批量拉 1000 条 → Bulk API 写入 ES → refresh_interval=30s

**Q46-21: ES 和关系型数据库的区别是什么？为什么告警检索用 ES 而不用 PostgreSQL？**

要点：
- ES 基于倒排索引，适合全文检索、多条件组合查询
- PostgreSQL 基于 B+ 树索引，适合精确查询、范围查询、事务
- 告警检索场景：
  - 全文搜索：搜索告警标题、描述中的关键词 → ES 倒排索引快
  - 多维过滤：按设备类型 + 严重级别 + 时间范围 + 状态组合查询 → ES filter cache 高效
  - 聚合统计：按严重级别分组统计、按设备类型分组统计 → ES aggregation 比 SQL GROUP BY 快
  - PostgreSQL 做这些：需要多个索引 + LIKE 模糊查询（走全表扫描）+ 多表 JOIN，性能差
- 数据量级：告警事件日均 2000+ 条 × 6 个月 = 几百万到上千万条，ES 毫秒级查询，PG 可能秒级

**Q46-22: ES 的倒排索引原理讲一下？**

要点：
- 正排索引：文档ID → 文档内容（传统数据库）
- 倒排索引：关键词 → 包含该关键词的文档ID列表
- 构建：分词 → 词条（Term）→ 倒排表（Term → DocID 列表 + 词频 + 位置）
- 查询："逆变器温度过高" → 分词 → "逆变器"+"温度"+"过高" → 各自查倒排表 → 交集 → 返回文档
- 比关系数据库 LIKE 快：LIKE '%逆变器%' 走全表扫描，ES 直接查倒排表定位文档
- SECP 告警场景：搜索"温度过高"告警 → ES 倒排索引秒级返回，PG LIKE 全表扫描慢

### 7.6 ClickHouse（⚠️ 真实角色：secp-prophet 告警/消息记录，非设备时序）

> **代码核实更正（2026-08-28，G:\Project\goodwe）**：SECP 的**数仓真实落点是 ClickHouse**——由 `secp-prophet` 的 `DW*DataSource` 用 `@KafkaListener` 消费 Kafka `secp-rich-*-event-data` 系列 topic（数仓 topic），转 `UnifiedMessage` 后写入，存的是**设备事件/状态/运行全量（即数仓本身）**，不是孤立的"告警日志"。**设备时序聚合曲线（功率/发电量/环测）在 PostgreSQL + TimescaleDB 超表**（`station_pv_power_5min_t0/t1`，主键 `(station_id, date_time)`，按天 chunk），是算法服务从 watchman 拉取的**聚合产物**，并非原始遥测直写。下表保留 ClickHouse 通用技术要点（面试可讲原理），SECP 分工描述已按真实代码修正。

**Q46-23: 为什么用 ClickHouse？（通用优势 + SECP 真实分工）**

要点：
- ClickHouse 通用优势：
  - 列式存储：只读取查询涉及的列，IO 少；行式存储要读整行
  - 向量化执行：批量处理一列数据，CPU 缓存命中率高
  - 数据压缩：列内数据类型一致，压缩比高（10:1），省存储
  - 聚合查询极快：亿级数据 GROUP BY 秒级返回
- SECP 真实分工：ES 存告警事件（全文检索），ClickHouse 是**数仓**（secp-prophet 消费 Kafka 数仓 topic，存设备事件/状态/运行全量，跨租户/跨周期多维分析），**设备时序曲线在 PG/TimescaleDB 超表**（聚合产物），各取所长

**Q46-24: ClickHouse 的 MergeTree 引擎一般怎么选？（通用要点）**

要点：
- ClickHouse 最常用引擎家族：MergeTree
- `ReplacingMergeTree` 适合需按版本去重的场景：
  - 按时间排序，重复数据自动去重
  - 去重发生在后台 Merge 时，不保证查询时已去重（需要 FINAL 关键字或 optimize 强制 merge）
- Partition by toYYYYMM(time)：按月分区，查询时分区裁剪
- Order by (deviceId, time)：设备ID 在前，同一设备数据连续存储，查询效率高
- TTL：`TTL time + INTERVAL 2 YEAR DELETE`，自动清理 2 年前数据
- 不用 Kafka 引擎直接消费 Kafka：先用 Kafka → 文件 → ClickHouse 批量导入，写入性能更好
- ⚠️ 以上为 ClickHouse 通用选型知识；SECP 设备时序实际在 PG/TimescaleDB 超表（主键去重），不依赖 ReplacingMergeTree

**Q46-25: ClickHouse 写入有什么注意事项？为什么不能频繁小批量写？**

要点：
- ClickHouse 不适合高频小批量写入（INSERT 100 条/次），适合低频大批量（INSERT 10000+ 条/次）
- 原因：每次 INSERT 生成一个 Part 文件，后台 Merge 合并 Part，Part 太多导致 Merge 跟不上，查询性能下降
- 正确做法：
  - Kafka 消费者攒到 10000 条或 10 秒，批量 INSERT
  - 或用 Buffer 表引擎（Buffer 表 → 满 10000 条自动 flush 到 MergeTree）
- SECP 做法：prophet 的 `DW*DataSource` 消费 Kafka 数仓 topic → 转换 → 批量写入 ClickHouse（注：本仓库未用 Flink，消费即 Spring Kafka）

**Q46-26: ClickHouse 和 Elasticsearch 的区别？为什么两个都用？**

要点：
- ES：全文检索（倒排索引）、模糊查询、多条件组合过滤 — 适合告警事件检索
- ClickHouse：列式聚合（列存+向量化）、大数据量 GROUP BY — 适合告警/消息记录多维分析
- 对比：
  | 维度 | ES | ClickHouse |
  |------|-----|-----------|
  | 索引 | 倒排索引 | 稀疏主键索引 + 排序 |
  | 擅长 | 全文检索、过滤 | 聚合统计、范围查询 |
  | 写入 | 准实时（1s refresh） | 批量写入（攒批） |
  | 压缩 | 一般 | 极高（10:1） |
  | 并发查询 | 高（支持大量并发） | 低（不适合高并发点查） |
- SECP 数据分工（已核实）：
  - 告警事件检索 → ES（需要全文搜索"温度过高"等关键词）
  - **数仓（设备事件/状态/运行全量） → ClickHouse（secp-prophet 消费 Kafka 数仓 topic）**
  - **设备功率/发电量曲线（聚合产物） → PostgreSQL + TimescaleDB 超表（T+0/T+1 双表），并非原始遥测直写**

---

### 7.7 MQTT 与物联网架构设计（综合追问）

> 本节约等于"物联网通信架构"专题，覆盖 MQTT 协议、物联网设计思考、安全认证、海量连接高可用、可靠性幂等，以及**MQTT 入站后的业务清洗与存储逻辑**；每个点尽量落到 SECP 真实代码（G:\Project\goodwe，2026-08-28 核实）。

#### 7.7.1 MQTT 协议核心

**Q（协议）：MQTT 为什么适合物联网？和 HTTP 比？**
- 轻量（固定头仅 2 字节）、基于 TCP 长连接、发布/订阅解耦、内建遗嘱/保留消息/QoS；HTTP 每次请求建连开销大、无服务端推送（只能轮询），不适合海量设备上行的同时又要下行控制。
- SECP：设备经 **EMQX**（MQTT Broker）上行，云端 `secp-manager` 订阅消费；边缘 `iot` 模块（`we/meter/up/{iot}` 等 topic scheme）在 `properties.toml` 里已定义。

**Q（协议）：QoS0/1/2 区别？你们怎么选？**
- QoS0 至多一次（fire and forget）；QoS1 至少一次（PUBACK 确认，可能重传）；QoS2 恰好一次（四步握手，最重）。
- SECP 选型：遥测上行用 **QoS1**（允许重传、配合去重，上行不可丢、允许少量重复）；下行控制/升级公告也用 QoS1 + 遗嘱。QoS2 因握手开销大，高频遥测不用。

**Q（协议）：遗嘱消息（Last Will）有什么用？**
- 设备异常断线时 Broker 自动代发遗嘱到预设 topic，平台立即感知"设备离线"。
- SECP：设备遗嘱 → `secp-manager` 收到 → 标记离线、触发告警/重连策略。

**Q（协议）：保留消息（Retained）和 clean session？**
- Retained：Broker 保留该 topic 最后一条，新订阅者立即拿到最新态。
- `cleanSession=false`：断线期间消息在 Broker 缓存，重连后补投（会话保持）。
- SECP：控制指令用 `cleanSession=false` + QoS1，保证设备断线期间的指令不丢。

**Q（协议）：topic 怎么设计？**
- 层级化：`secp/{deviceType}/{deviceId}/telemetry`、`secp/{deviceId}/control`、`we/meter/up/{iot}`。
- 用通配符（`+` 单级、`#` 多级）批量订阅；topic 带 deviceId 便于路由与 ACL 鉴权。

#### 7.7.2 物联网架构设计思考

**Q（架构）：物联网平台整体怎么分层？**
- 设备层（采集器/网关）→ 连接层（MQTT Broker/EMQX，连接管理、鉴权、路由）→ 接入层（协议解析、边缘计算）→ 平台层（设备建模、规则引擎、时序存储、数仓）→ 应用层（监控/报表/结算/告警）。
- 边云协同：边缘做协议解析+边缘计算（Rust `formula` 引擎把裸寄存器换算成物理量），云端做聚合+存储+业务。

**Q（架构）：为什么用 MQTT 而不是直接 TCP 私有协议？**
- MQTT 是标准、生态成熟（EMQX/Paho），天然发布订阅解耦，内建 QoS/遗嘱/保留，便于水平扩展与多消费者复用；私有 TCP 协议要自己造轮子（帧格式、心跳、重传、路由）。

**Q（架构）：边缘计算和云端处理怎么分工？**
- 边缘（靠近设备）：协议转换（Modbus/IEC104→MQTT）、寄存器→物理量换算、断网本地缓冲（SQLite 按天分库）、无锁实时缓存（1 万+点/秒）。
- 云端：统一接入、清洗、聚合、时序存储、业务。
- 原则："**分层就近**"——能在边缘做的（单位/位域换算）就地做，必须集中做的（维度关联、全局去重、跨设备聚合）放云端。

**Q（架构）：设备太多、协议不统一怎么办？**
- 接入层做协议归一：不同型号/协议（Modbus RTU/TCP、IEC104、私有）统一收敛成标准指标模型 `{deviceId, metricKey, value, unit, ts}`，**映射表驱动**（加设备型号只改配置不改代码）。

#### 7.7.3 安全与认证设计

**Q（安全）：设备接入怎么防止伪造/非法设备？**
- 双向 TLS（mTLS）：设备持客户端证书，Broker 校验；SECP 用**自定义 CA**（证书从 OSS 拉取，`secp-manager` 的 `MqttConfiguration` 里配置）。
- 设备身份：clientId=设备唯一标识（SN），Broker 侧 ACL 限制该 clientId 只能发布/订阅自己 deviceId 下的 topic。
- EMQX 鉴权：username/password 或 JWT + ACL 规则；未注册设备拒绝连接。
- 白名单兜底：云端清洗层再校验 deviceId 是否在注册表（prophet SN **布隆过滤**挡未知设备）。

**Q（安全）：topic 级权限怎么控制？**
- EMQX ACL：按 clientId/用户名配置"允许发布/订阅哪些 topic 正则"，防止设备 A 仿冒发布设备 B 数据（越权发布）。

**Q（安全）：证书/密钥怎么管理、轮换？**
- 证书由 CA 签发、设备烧录；定期轮换（OSS 下发新 CA/证书）；密钥不硬编码，走配置中心/密钥管理。

**Q（安全）：传输安全之外，数据本身怎么防篡改/重放？**
- payload 设备侧签名、平台验签；关键控制指令带 nonce + 签名防重放。

#### 7.7.4 海量连接与高可用

**Q（高可用）：百万级长连接怎么扛？**
- Broker 集群：EMQX 集群（节点间一致性哈希分片连接），水平扩容。
- 连接层无状态 + 共享订阅（Shared Subscription）把消息均衡分发到多个消费节点。
- 资源：每 TCP 连接内存约几 KB（EMQX 基于 Erlang/异步），调内核参数（文件句柄、端口范围、TCP backlog）。
- SECP：EMQX 承担百万长连接、会话保持、QoS 重传——**Broker 不碰业务，只做传输**。

**Q（高可用）：设备断线重连怎么设计？**
- 指数退避重连（避免重连雪崩）；`cleanSession=false` 保证会话恢复后补投离线消息；遗嘱感知离线。

**Q（高可用）：限流与降级？**
- Broker 级限流（单 client 发布频率、连接速率）；平台消费侧背压（Kafka 缓冲削峰、消费者手动 ack 控制速率）；高峰降级（非关键指标降采样）。

**Q（高可用）：消费端怎么水平扩展？**
- Kafka 多分区 + 消费者组并发；按 `deviceId` keyBy 保证同设备消息有序、可 rescale（加分区只能加不能减，前期预留）。

#### 7.7.5 可靠性与幂等

**Q（可靠）：消息会丢吗？端到端怎么保证不丢？**
- 四段式：MQTT QoS1 + Broker 会话保持（断线补投）→ Kafka 多副本（acks=all + min.insync.replicas）→ 消费手动 ack（失败不提交 offset）→ 落库主键幂等。任一环断了，下一环都有恢复手段。

**Q（可靠）：重复消息怎么处理？**
- 传输层：QoS1 重传产生重复，用 `messageId`/`kafkaUuid` 布隆过滤或 Redis 短窗口去重。
- 落库层：主键 `(deviceId/stationId, ts)` + UPSERT / `ON CONFLICT DO NOTHING` 物理挡重。
- 收敛层：T+1 重算覆盖迟到/重复，最终一致。

**Q（可靠）：消息顺序怎么保证？**
- 同设备消息按 `deviceId` 路由到同一 Kafka 分区（keyBy），消费端单线程处理该分区 → 保序；跨设备无需全局顺序。

**Q（可靠）：at-least-once vs exactly-once，怎么取舍？**
- 物联网遥测用 **at-least-once + 幂等去重**（成本低、够用）；强一致场景（结算）走事务消息/对账。引入 Flink exactly-once 成本高，SECP 用 Spring Kafka + 主键幂等已满足（详见 7.4）。

#### 7.7.6 MQTT 入站后的业务清洗与存储逻辑（重点：洗和存怎么做）

> 面试官最爱追问"数据进来后到底怎么处理"。以 SECP 真实实现给出**完整逻辑链路**（分层就近）。

**入站到落库的完整逻辑：**

```
EMQX 收到设备上行 (topic: we/meter/up/{iot})
   │  secp-manager 入站适配器订阅
   ▼
① 接入与路由（MqttConfiguration + MessageHandlerFactory）
   - MqttPahoMessageDrivenChannelAdapter 入站订阅 defaultTopic
   - @ServiceActivator 收到消息 → MessageHandlerFactory.getMessageHandler(topic) 按 topic 路由到对应 handler
   - TLS：自定义 CA（证书从 OSS 拉取）做双向认证
   ▼
② 校验（洗·L0）
   - 设备合法性：deviceId 在注册表 / clientId 白名单（未注册拒绝）
   - schema：payload 必填字段存在、类型可解析
   - 时间合理性：ts 不为未来、不过旧
   ▼
③ 标准化（洗·L1）
   - 时区→UTC、单位归一（kW→W）、协议归一成统一指标模型
   - 边缘已做寄存器→物理量换算（Rust formula），上行即物理量
   ▼
④ 去重（洗·L2）
   - 传输去重：messageId/kafkaUuid 布隆过滤
   - 业务主键去重：落库主键 (deviceId, metricKey, ts) 幂等
   ▼
⑤ 转发 / 分发
   - 实时流 → 推 Kafka（削峰、多消费者复用）：
       · 数仓 topic：secp-prophet DW*DataSource 消费 → 清洗(过滤 status null + SN 布隆 + 解析插件关联补全) → 写 ClickHouse（数仓）
       · 业务 topic：secp-manager 消费设备关系/操作日志
   - 事件型（告警）→ Kafka → secp-watchman → ES
   ▼
⑥ 聚合与落库（存）
   - 时序曲线：secp-algorithm StationMetricScheduleServiceImpl @Async 定时从 watchman 拉
     → 5min 聚合 → saveBatch 写 PG/TimescaleDB 超表 station_pv_power_5min_t0(实时)/_t1(T+1)
   - 质量兜底：data-config 缺失检测(DwMissingDataInfo) + 补录(MakeupRuleEnum: COPY/AVG/PERCENT/AI) + 异常标签(InconsistentTagEnum)
   ▼
⑦ 对外（存之后怎么被用）
   - 数仓统一查询 API：业务/框架按设备+时间+agg+标签取数（业务有时直调）
   - PG/TimescaleDB 时序库：测试期直接查 raw/dwd/dws 超表核对
```

**Q（洗存）：为什么"洗"要分这么多层，而不是一步到位？**
- 分层就近：越靠近接入越先做廉价校验（schema/设备合法性），重计算（维度关联、聚合）后置；每层只处理自己职责内的脏数据，下游拿到的是上一层已干净数据，便于定位问题、单独优化、失败隔离。

**Q（洗存）：清洗在哪一层做，边缘还是云端？**
- 边缘做第一道（单位/位域换算、断网缓冲）；云端做完整清洗（空值过滤、设备白名单、解析关联、正则校验、主键去重、T+1 收敛）。详见《数据清洗_链路实现详解.md》《数据清洗方案设计文档.md》。

**Q（洗存）：数据最终存在哪几类库，分别存什么？**
- **ClickHouse（数仓）**：prophet 消费 Kafka 数仓 topic，存设备事件/状态/运行全量
- **PostgreSQL + TimescaleDB 超表**：设备功率/发电量聚合曲线（T+0/T+1）
- **Elasticsearch**：告警事件（全文检索）
- **Redis**：实时缓存、调度幂等标记

**Q（洗存）：清洗失败的数据去哪了？**
- 校验不通过（伪造设备/格式错误）→ 直接丢弃 + 告警日志
- 质量可疑（越界/突变）→ 标 `quality=SUSPECT` 保留可过滤
- 缺失 → 写 `quality=MISSING` 占位交补录
- ES 写入失败 → 从下游剔除
- 脏数据不进主库，只在数据质量监控里体现（接入成功率/清洗丢弃率/去重率/缺失率/异常率）

**Q（洗存）：如果让你从零设计这套"MQTT 上行 → 清洗 → 存储"，你会怎么搭？**
- 见《数据清洗方案设计文档.md》：设备上传 → 数仓接入层（L0 校验/L1 标准化/L2 去重/L3 质量/L4 维度补全/L5 落库）→ PG/TimescaleDB 超表（raw/dwd/dws）→ 数仓统一查询 API → 业务框架 → 业务应用；业务高频取数可直调数仓接口。

---

### 7.8 TimescaleDB 时序库深度追问

> **定位**：SECP 设备时序（功率/发电量/环测曲线）的真实存储后端是 **PostgreSQL + TimescaleDB**（`station_pv_power_5min_t0/t1` 等超表）。本章把 TimescaleDB 从"是什么"到"怎么用、怎么调优、怎么选型"讲透，应对面试官从原理到落地的层层追问。

#### 7.8.1 什么是时序数据？为什么不能用普通表硬扛

**Q：时序数据有什么特征，和普通业务数据有什么区别？**
> 一条时序数据 = 时间戳 + 标识（设备/场站）+ 指标值。SECP 里就是"某场站某时刻的有功功率"。特征：**只追加、几乎不更新、写多读少、按时间范围聚合查询、数据量随时间线性增长**（千万级设备 × 每天 = 几十亿行）。
> 普通 PG 表硬扛的问题：① 单表几十亿行，B 树索引膨胀、VACUUM 跟不上；② 按时间范围查询要全表扫历史分区；③ 没有原生压缩，存储爆炸；④ 没有预聚合，每次 GROUP BY 都现场算。TimescaleDB 就是给 PG 打上"时序外挂"解决这些。

**Q：TimescaleDB 和直接分库分表（ShardingSphere）比，好在哪？**
> 这是 SECP 的真实选型故事（不靠 ShardingSphere）。TimescaleDB 用 **hypertable 自动按时间分 chunk**，对应用层透明——业务 SQL 还是写一张"逻辑表"，不用自己管分片键、路由、跨片聚合。分库分表要自己处理：分片路由、跨片 GROUP BY、扩容再平衡（分片数只能加不能减，前期要预留）。hypertable 的 chunk 随时 drop、压缩、TTL，运维成本远低于分库分表。

#### 7.8.2 hypertable 原理（高频考点）

**Q：hypertable 和 chunk 是什么关系？查询怎么加速？**
> hypertable 是**逻辑表**，底层按时间维度自动切成多个 **chunk**（物理表，默认按 `chunk_time_interval` 切，SECP 用 `INTERVAL '1 day'`）。
> 加速原理 = **分区裁剪（partition pruning）**：查询带时间条件时，规划器只扫涉及的几个 chunk，不碰历史。例如查"今天某场站功率"，只扫当天 1 个 chunk；查"本月"扫 30 个 chunk。chunk 数量可控（旧 chunk 压缩/drop 后，热数据常驻的 chunk 很少），索引也小，查询快。

**Q：chunk_time_interval 怎么定？切太细或太粗会怎样？**
> 经验值：单 chunk 行数在 **25 万~几百万行**最佳。太细（如 1 小时）→ chunk 过多，元数据膨胀、查询要扫很多小 chunk；太粗（如 1 年）→ 单 chunk 过大，分区裁剪失效、VACUUM/压缩慢。SECP 用 1 天，配合"每天几百万点"的量级刚好。也可按空间再分区（`partition_dimension`，如按 `station_id` 哈希再分），避免单时间点 chunk 过大。

**Q：create_hypertable 后，索引怎么建？**
> hypertable 会自动给时间列建索引，且**每个 chunk 独立建索引**。业务主键/查询键要建在 `(station_id, date_time)` 这类"维度+时间"组合上——查询"某场站某时间段"走主键索引且命中分区裁剪，双加速。注意：不要在超表上建全局唯一索引跨 chunk 会有代价，主键要含时间列（否则跨 chunk 唯一约束失效）。

#### 7.8.3 连续聚合（Continuous Aggregates）—— 对应 T+0/T+1 的核心机制

**Q：连续聚合是什么？和物化视图有什么区别？**
> 连续聚合是 TimescaleDB 的**自动增量刷新的预聚合**，专治"每次查都 GROUP BY 历史"。本质是基于 hypertable 的物化视图，但比普通物化视图强：① **自动增量刷新**（只刷新上次到现在的增量，不重算全量）；② 后台按 `refresh_lag` 调度；③ 查询自动路由到聚合表。
> SECP 对应：T+0 实时曲线由实时写入的 `_t0` 超表提供；**T+1 的重算收敛可由连续聚合自动维护**——定义基于 `_t0` 的连续聚合（如 1 天粒度），离线重算时刷新落 `_t1`。这样"T+1 通过 T+0 再次计算"在 TimescaleDB 里有原生机制支撑，不必全靠自己写批任务。

**Q：连续聚合的 refresh_lag / refresh_interval 怎么配？**
> `refresh_lag` = 滞后多久才刷（如 `INTERVAL '1 hour'`，避免刷正在写入的热 chunk，防部分数据被锁）；`refresh_interval` = 后台刷新频率。SECP T+1 场景可设 `lag = 1 day` 或更紧，按离线对账时效要求定。实测：连续聚合查询比原表 GROUP BY 快 1~2 个数量级。

#### 7.8.4 压缩（列式压缩，省存储关键）

**Q：TimescaleDB 的压缩怎么工作？压缩后还能查吗？**
> 开启后，chunk"超过某年龄"（如 7 天）会被压缩：**按 `segmentby`（分组列，如 `station_id`）+ `orderby`（排序列，如 `date_time`）做列式压缩**，多个原始行压成一行内的列式数组。压缩比通常 **10:1~20:1**（SECP 的量级下非常可观）。
> 压缩后**仍可查**——查询时透明解压，只是不能单行 UPDATE（需先解压 chunk 改完再压）。所以时序"只追加"特性正好契合：热 chunk 不压缩可写，冷 chunk 压缩省空间。
> SECP 用法：`ALTER TABLE ... SET (timescaledb.compress, timescaledb.compress_segmentby='station_id', timescaledb.compress_orderby='date_time DESC')`。

**Q：segmentby / orderby 怎么选？选错会怎样？**
> `segmentby` 选**查询过滤最频繁、且希望按它保留行的列**（如 `station_id`）——同一场站的数据压在一起，按场站查时解压少。`orderby` 选**排序列**（如时间 DESC），让压缩块内数据有序，范围扫描跳块快。选错：segmentby 选了高基数列→压不紧；orderby 选错→范围查询跳块失效，反而变慢。

#### 7.8.5 保留策略 / TTL / 数据生命周期

**Q：历史数据怎么清理？直接 DELETE 行吗？**
> 绝对不直接 DELETE——几十亿行 DELETE 会锁表、产生巨量 dead tuple。用 TimescaleDB 的 **`drop_chunks`**（直接丢弃整块物理文件，毫秒级）配合保留策略：`SELECT drop_chunks('station_pv_power_5min_t0', INTERVAL '2 years')`（SECP TTL 2 年）。也可 `add_retention_policy` 自动按策略 drop。这就是"保留策略一行配置"优于业务库手删。

**Q：数据要保留但想降本怎么办？**
> 分层：热数据（近期）不压缩常驻 SSD；冷数据压缩 + 可降到对象存储/廉价盘。TimescaleDB 企业版有 tiered storage，社区版靠压缩 + drop_chunks 组合。SECP 用"压缩 + 2 年 TTL"足够。

#### 7.8.6 写入策略（攒批 / UPSERT / 幂等）

**Q：TimescaleDB 写入要注意什么？为什么必须攒批？**
> 时序写入是**小批量高频 append**，单条 INSERT 在 PG 上代价极高（每条都要走 WAL、建索引）。必须**攒批**（如每 5s / 每 1000 条一批 `COPY` 或 `INSERT ... VALUES (...),(...)`）。SECP 做法：`secp-algorithm` 的 `saveBatch` 批量写超表。
> 幂等：主键 `(station_id, date_time)` + `ON CONFLICT DO NOTHING / UPDATE`（UPSERT），重试安全——MQTT QoS1 重传、消费位移重置重放都不会产生重复行。这就是"去重三层兜底"在存储层的硬保证。

**Q：写入热点/瓶颈怎么破？**
> 时序写入天然按时间单调递增，所有写入打在**最新的一个 chunk**——单点写入瓶颈。优化：① 按空间再分区（`partition_dimension=station_id`）把写入分散到多个 chunk；② 攒批降低 WAL 次数；③ 连接池（PgBouncer）复用；④ 必要时多节点版（TimescaleDB Multi-node，存取分离）。SECP 量没到这步，单节点 + 攒批 + 主键幂等够用。

#### 7.8.7 查询性能与慢查排查

**Q：一条"某场站本月每小时发电量"的 SQL 怎么写最快？**
```sql
-- 走 T+1 聚合表（已 5min 预聚合），不回原表
SELECT date_trunc('hour', date_time) AS hh, avg(p) AS p_avg
FROM station_pv_power_5min_t1
WHERE station_id='ST001'
  AND date_time >= '2026-08-01' AND date_time < '2026-09-01'
GROUP BY hh ORDER BY hh;
```
> 快的三点：① 查 `_t1` 聚合表（行数少）；② `station_id` 走主键索引 + 分区裁剪（只扫 8 月 chunk）；③ 不碰 raw。慢查排查：`EXPLAIN (ANALYZE, BUFFERS)` 看是否**全 chunk 扫**（时间条件缺失）、索引是否命中、内存命中率。

**Q：分区裁剪没生效，怎么排查？**
> 常见原因：① 查询没带时间列条件 → 必须带 `date_time`；② 时间列被函数包了（如 `date(date_time)`）→ 用范围比较别包函数；③ 类型不匹配（`date_time` 是 timestamptz，传了 date 没显式转换）；④ chunk 太大导致裁剪后仍慢 → 调小 `chunk_time_interval`。

#### 7.8.8 选型对比（面试必问）

**Q：为什么选 TimescaleDB 而不是 InfluxDB / ClickHouse / TDengine？**
> | 维度 | TimescaleDB | InfluxDB | ClickHouse | TDengine |
> |------|------------|-----------|------------|----------|
> | 模型 | PG 超表 + SQL | measurement+tags+fields | 表 + SQL | 超级表 + SQL 扩展 |
> | 降采样 | 连续聚合（原生） | Retention + CQ（原生） | 物化视图（自搭） | 自带 |
> | 联表分析 | 强（标准 SQL join 维表） | 弱 | 强 | 中 |
> | 事务 | 有（PG 原生） | 无 | 弱 | 弱 |
> | 生态 | 与业务库同 PG，运维栈复用 | 独立 | 独立 | 独立 |
> - SECP 选 TimescaleDB 的核心理由：**① 业务库本就是 PG，时序库复用同一套 PG 运维栈、备份、连接池，少维护一个组件；② 需要把时序数据和设备档案/电站/租户维表 JOIN 做业务分析，标准 SQL 直接搞定（InfluxDB 类 SQL 接不住）；③ 偶尔要补录/修正历史，PG 事务原生支持；④ 连续聚合原生做 T+1 预聚合**。
> - ClickHouse 在 SECP 另有用途（secp-prophet 数仓存告警/消息事件记录），不是时序库——两者并存不冲突。

**Q：TimescaleDB 和 ClickHouse 在 SECP 的边界？**
> 时序曲线/聚合（功率、发电量、环测）→ **TimescaleDB**（T+0/T+1 超表，按场站+时间查询）；告警/消息事件记录（设备事件、状态变更、推送记录，跨租户跨周期多维分析）→ **ClickHouse**（secp-prophet 消费 Kafka 数仓 topic 落库）。一句话：**"随时间的数值曲线"用 TSDB，"海量事件记录的分析"用 ClickHouse**。

#### 7.8.9 真实 SECP 证据速记

- 真实表：`algorithm.station_pv_power_5min_t0`（注释"t+0 5min曲线"）、`station_pv_power_5min_t1`（"t+1 5min曲线"），同款还有 `station_load_power_5min_t0/t1`、`station_env_tag_5min_t0/t1`
- DDL：`create_hypertable(..., 'date_time', chunk_time_interval => INTERVAL '1 day')`，主键 `(station_id, date_time)`
- 写入：`secp-algorithm` `StationMetricScheduleServiceImpl` `@Async` 调度从 watchman 拉数据聚合 `saveBatch` 写 `_t0/_t1`
- 测试核对：直接查 PG 时序库（tsdb 库）的 `raw/dwd/dws/t0/t1` 超表

---

## 第八章 DevOps 与监控

**Q48: 你们 Docker + K8s 部署的流程是什么？**

要点：
- 代码提交 → CI 构建 JAR → Dockerfile 构建镜像 → Push Harbor → K8s 拉取部署
- Dockerfile 中 JVM 参数：`-XX:MaxRAMPercentage=75.0` 容器内内存感知
- jemalloc 替代 glibc malloc 解决容器内存碎片
- SkyWalking Agent 通过 `javaagent` 挂载，无侵入

**Q49: Prometheus + SkyWalking 分别监控什么？**

要点：
- Prometheus：基础设施指标（CPU/内存/磁盘/网络），应用指标（JVM GC/线程池）
- SkyWalking：全链路追踪（Trace），服务拓扑图，慢接口定位
- Grafana：可视化面板
- 告警联动：Prometheus AlertManager → 触发告警 → 告警引擎处理

**Q50: 你说核心接口可用性 99.9%，P99 在 500ms 以内，怎么保证的？**

要点：
- 多级缓存降低 DB 压力
- Sentinel 限流熔断防止雪崩
- K8s 多副本 + 健康检查自动恢复
- SkyWalking 慢接口监控 + 告警
- 数据库慢 SQL 定期排查优化

---

## 第九章 Java 基础与并发

**Q51: Java 8 的 Stream 你在项目中怎么用的？**

要点：
- 集合转换：`List<Device> → Map<String, Device>` 用 `Collectors.toMap()`
- 过滤+映射：`devices.stream().filter(d -> d.getStatus() == 1).map(Device::getId).collect(toList())`
- 分组：`Collectors.groupingBy(Device::getType)`
- 并行流：`parallelStream()` 用于大数据量处理（谨慎使用）

**Q52: 你说熟练掌握多线程并发，ThreadPoolExecutor 的核心参数有哪些？**

要点：
- corePoolSize / maximumPoolSize / keepAliveTime / workQueue / threadFactory / rejectHandler
- 任务调度流程：核心线程 → 队列 → 非核心线程 → 拒绝策略
- SECP 中 `ThreadPoolConfig` 配置了 6 个隔离线程池（不同业务不共用）

**Q53: 拒绝策略有哪些？你们怎么选的？**

要点：
- AbortPolicy（默认）：抛异常
- CallerRunsPolicy：让提交者执行
- DiscardPolicy：丢弃
- DiscardOldestPolicy：丢弃最老的
- SECP 中用 CallerRunsPolicy + 有界队列，让调用方降速而非丢消息

**Q54: CompletableFuture 你在项目中用过吗？**

要点：
- 多个第三方接口并行调用（天眼查、天气 API），用 `allOf` 等待全部完成
- 超时控制：`orTimeout()` + `exceptionally()` 降级
- 异步编排：`thenApply` / `thenCompose` 链式处理

**Q55: ThreadLocal 在你们项目中用在哪？有什么内存泄漏风险？**

要点：
- TraceId 传递：`TraceIdFilter` 通过 ThreadLocal 存 TraceId，日志框架从 ThreadLocal 取
- 内存泄漏：线程池复用线程，ThreadLocal 不清理会导致 Entry 泄漏
- 解决：`try-finally` 中 `ThreadLocal.remove()`
- ThreadLocalMap 的 key 是弱引用，value 是强引用，value 不会被自动回收

**Q56: CAS 和 AQS 原理？**

要点：
- CAS：Compare And Swap，CPU 级别原子操作 `cmpxchg` 指令
- ABA 问题：通过版本号解决（AtomicStampedReference）
- AQS：volatile state + CLH 变种队列，ReentrantLock/Semaphore/CountDownLatch 底层

**Q57: 你简历里提到设计模式，策略模式在结算中怎么用的？**

要点：
- 不同电价类型（分时电价、阶梯电价、固定电价）实现统一接口 `SettlementStrategy`
- 工厂模式 `SettlementStrategyFactory` 根据协议类型创建策略实例
- 新增电价类型只需新增策略类，符合开闭原则

---

## 第十章 固德威 SECP 业务深度追问

### 10.1 电费结算

**Q58: 结算全链路从协议管理到发票开具，完整讲一遍。**

要点：
- 协议管理：定义电价类型、结算周期、收付款方
- 结算单生成：定时跑批（Cron 定时任务）+ 手动创建
- 自检规则校验：金额校验、日期校验、重复校验
- 多级审批流：Flowable 流程，部门→财务→总监
- 账单管理：生成 PDF 账单
- 在线支付：对接第三方支付
- 发票开具：对接百旺/用友电子发票

**Q59: 结算幂等怎么保证的？重复跑批会不会重复结算？**

要点：
- Redisson 分布式锁：同一协议同一周期的结算单生成互斥
- 数据库唯一约束：协议ID + 结算周期 + 账期 唯一索引
- 幂等 Token：前端提交带 token，后端校验
- RocketMQ 消费端幂等：通过结算单号做消费去重

**Q60: 结算单生成是定时跑批，如果数据量很大怎么优化？**

要点：
- 分页处理：每次查 1000 条协议，避免一次性加载
- 多线程：CompletableFuture 并行处理不同协议的结算
- 写入优化：批量 INSERT（MyBatis-Plus saveBatch）
- 避锁化：不同协议互不影响，用协议ID做锁粒度

### 10.2 告警引擎

**Q61: 事件库→事件→消息三级配置是什么意思？**

要点：
- 事件库（Event Library）：定义告警类型模板（如"逆变器离线"、"电压异常"）
- 事件（Event）：设备实际产生的告警实例（绑定设备 + 时间 + 指标值）
- 消息（Message）：告警通知（通知渠道 + 通知人 + 通知内容）
- 三级解耦：新增告警类型不改代码，只改事件库配置

**Q62: 告警的恢复机制怎么实现的？**

要点：
- 设备自动恢复：设备重新上线后上报状态，匹配恢复规则，自动关闭告警
- 管理员手动解除：人工判断后手动关闭
- 恢复通知：告警关闭后发恢复通知（同渠道触达）

**Q63: 多渠道触达（企业微信/钉钉/短信/邮件/飞书）怎么设计的？**

要点：
- 策略模式：`NotificationStrategy` 接口，每个渠道一个实现类
- 责任链：告警 → 匹配通知规则 → 依次调用各渠道策略
- 异步发送：RocketMQ 消息异步触发，不阻塞告警主流程
- 失败重试：渠道发送失败进死信队列，指数退避重试

### 10.3 AI Agent

**Q64: Agent 怎么和告警引擎联动的？**

要点：
- 告警引擎产生告警事件 → Kafka 消息 → Agent 消费
- Agent 自动分析：LLM 根据告警类型 + 设备信息 + RAG 检索 SOP
- 输出：告警分类 + 根因分析 + 处置建议
- 飞书机器人推送分析结果到运维群
- 运维人员可以多轮对话追问

**Q65: RAG 知识库里都放了什么？怎么维护的？**

要点：
- 运维 SOP（标准操作流程）：每种告警类型的处理步骤
- 历史故障案例：之前的故障记录和解决方案
- 设备手册：逆变器/储能设备的技术参数
- 维护：运维人员手动录入 + Agent 对话自动沉淀

### 10.4 其他

**Q66: secp-we-app 作为 BFF 层，和直接前端调各微服务有什么区别？**

要点：
- BFF 聚合多服务数据，前端一次请求拿到完整数据
- BFF 做数据裁剪，只返回前端需要的字段
- BFF 层做缓存，减少下游服务压力
- BFF 隔离前端和后端服务变化

**Q67: 你们怎么对接这么多第三方系统的？（天眼查/天气/政府/充电桩）**

要点：
- 统一第三方对接 SDK 模块
- 适配器模式：每个第三方一个 Adapter
- 超时 + 降级：CompletableFuture 超时降级返回缓存数据
- 限流：Sentinel 限制第三方调用频率

---

## 第十一章 江苏风云业务追问

**Q68: 芋道框架（yudao）你了解多少？为什么选它？**

要点：
- 芋道是开源的 Spring Boot 快速开发框架，含权限/代码生成/工作流
- 选它因为项目周期短（8 个月），需要快速交付
- 框架自带 RBAC 权限、数据权限、多租户
- 在此基础上做业务开发，节省基础设施开发时间

**Q69: 多数据源配置怎么做的？有什么坑？**

要点：
- Spring Boot + dynamic-datasource-spring-boot-starter
- `@DS("slave")` 注解切换数据源
- 坑：事务跨数据源不生效（需要分布式事务或拆分事务）
- 坑：连接池配置每个数据源独立，注意连接数

**Q70: 标签匹配算法怎么设计的？**

要点：
- 多级标签：行业标签 → 领域标签 → 细分标签
- 权重计算：投资者标签权重 × 项目标签权重，加权求和
- Redis ZSet 存标签热度，加速匹配查询
- 缓存策略：热门标签结果缓存，长尾标签实时计算

---

## 第十二章 上海精辰业务追问

**Q71: 单体向微服务迁移，最大的挑战是什么？**

要点：
- 数据库拆分：原 500+ 张表，按业务域拆分，跨服务 JOIN 改成 RPC 调用
- 事务问题：本地事务变成分布式事务，用消息队列保证最终一致
- 接口改造：同步调用改异步，需要处理回调
- 灰度迁移：新老系统并行运行，逐步切换流量

**Q72: 你说同步接口改 RabbitMQ 异步，原来的线程池耗尽问题怎么解决的？**

要点：
- 原方案：同步插入，高峰期请求数 > 线程池最大值 → 请求排队 → 超时
- 新方案：请求写入 RabbitMQ 立即返回，消费者按节奏处理
- 线程池释放快，接口响应时间稳定
- 消费者端可横向扩展（多实例消费）

**Q73: 报表 ETL 方案的具体实现？**

要点：
- 原方案：查询时实时 JOIN 20+ 张流程表，生成报表需要 30s+
- 新方案：Quartz 定时任务每天凌晨跑 ETL
- 流程：流程表数据 → 提取 → 按报表维度转换 → 写入报表表
- 报表查询变成单表查询，毫秒级返回

**Q74: 聚合支付和对账怎么做的？**

要点：
- 统一支付接口：前端调统一接口，后端路由到支付宝/微信/银行
- 异步回调：支付渠道回调通知支付结果
- 对账：定时拉取各渠道账单，和系统订单逐笔比对
- 差错处理：长款（渠道有系统无）→ 补单；短款（系统有渠道无）→ 退款

**Q75: MuleSoft 在你们项目里做什么？**

要点：
- MuleSoft 是 ESB/集成中间件
- 统一路由：多家医院的接口通过 MuleSoft 统一暴露
- 协议转换：HTTP ↔ WebService ↔ HL7
- 消息转换：不同医院的数据格式统一

**Q76: 日志从数据库表升级为 ELK，改造了什么？**

要点：
- 原：Log 写数据库表，查询慢，影响业务库性能
- 新：Logback → Logstash → Elasticsearch → Kibana
- 日志和业务数据分离，DB 压力下降
- 支持全文检索 + 时间范围查询，第三方接口排查效率提升

---

## 第十三章 南京紫金业务追问

**Q77: SpringSecurity + JWT + Redis 的登录流程？**

要点：
- 登录：用户名密码校验 → 生成 JWT（含用户ID + 角色） → JWT 存 Redis（可主动失效）
- 请求：Filter 拦截 → 解析 JWT → 校验 Redis 是否存在 → 设置 SecurityContext
- 登出：删除 Redis 中的 JWT（JWT 本身无法主动失效，靠 Redis 控制）
- 为什么用 Redis：JWT 无状态但无法主动失效，Redis 补充了主动失效能力

**Q78: Activiti 工作流在审核端怎么用的？**

要点：
- 申报端提交 → 创建 Activiti 流程实例 → 审核端代办
- 流程节点：初审 → 复审 → 终审
- 代办查询：`TaskService.createTaskQuery().taskAssignee(userId).list()`
- 审批：`taskService.complete(taskId, variables)`

**Q79: AOP 实现日志管理，具体怎么做的？**

要点：
- 自定义注解 `@Log`
- `@Aspect` + `@Around` 拦截注解方法
- 记录：操作人、操作时间、方法名、参数、返回值、异常信息
- 异步写入：通过线程池异步写入日志表/ELK

**Q80: SQL 调优从 3s 到 0.2s，能具体说哪条 SQL 吗？**

要点：
- 场景：审核端代办列表查询，关联 5+ 张表（用户/角色/部门/流程实例/任务）
- 问题：无索引 + 大量 JOIN + 子查询
- 优化：
  - JOIN 字段加索引
  - 子查询改为 JOIN
  - 分页优化：先查主表 ID，再 JOIN 明细
  - 避免函数操作索引字段（如 DATE_FORMAT）

---

## 第十四章 综合场景设计题

**Q81: 如果让你从零设计 SECP 的电费结算系统，你怎么做？**

要点：
1. 领域建模：协议、结算单、账单、支付、发票五个聚合根
2. 核心流程：协议管理 → 结算单生成 → 审批流 → 账单 → 支付 → 发票
3. 幂等设计：Redisson 锁 + 唯一约束 + 消息消费幂等
4. 异步对账：RocketMQ 事务消息
5. 扩展性：策略模式支持新电价类型，Flowable 支持新审批流
6. 监控：结算耗时、失败率、对账差异率

**Q82: 如果告警系统 QPS 突然翻 10 倍，你怎么应对？**

要点：
1. Kafka 分区扩容 + 消费者扩容
2. ES 写入端 buffer + bulk flush
3. Sentinel 限流保护核心服务
4. Flink 侧增加并行度
5. 降级策略：非关键告警延迟处理

**Q83: 如果让你重新设计 AI Agent，你会做什么改进？**

要点：
1. 多 Agent 协作：分类 Agent + 诊断 Agent + 处置 Agent 分工
2. 知识库自动更新：Agent 处置后自动沉淀案例到知识库
3. 主动巡检：不只是被动响应告警，定期分析设备趋势
4. 评估体系：建立 Agent 回答准确率的评估 pipeline

**Q84: 如果你们的结算系统和银行系统做对账，发现差异怎么排查？**

要点：
1. 差异类型分类：时间差（银行 T+1，系统 T+0）、金额差、状态不一致
2. 自动对账：系统订单 ←→ 银行账单逐笔比对
3. 差异处理：自动重试 → 人工介入
4. 数据补偿：RocketMQ 消息补偿最终一致

**Q85: 如果让你设计一个千万级设备的数据管道，技术选型怎么做？**

要点：
1. 采集层：MQTT Broker（EMQX）集群
2. 缓冲层：Kafka（按设备ID分区）
3. 处理层：Flink（实时清洗 + 规则引擎）
4. 存储层：ClickHouse（时序统计）+ ES（全文检索）
5. 查询层：BFF + Redis 缓存
6. 监控：Prometheus + SkyWalking 全链路
